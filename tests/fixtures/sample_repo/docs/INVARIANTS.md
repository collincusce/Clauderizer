# Invariants

Rules that hold across all work. Append-only. Numbered `INVARIANT-NN`.

## Invariants

### INVARIANT-01 — Markdown is canonical

The graph index is a disposable cache; markdown always wins.
