# Data Sources

External data and APIs the project depends on. Each with a risk profile.
Tracked as `ext.<name>` entities (`cz_upsert_entity --type external-service`).

## Services

_(Add external services here.)_
