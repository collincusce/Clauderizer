# Cascade Report: subsys.markdown-core status active -> active

> Generated: 2026-06-09T21:04:09Z

## Trigger

`subsys.markdown-core` — status active -> active

## Affected entities

### Direct dependents

- **subsys.graph** (status: active) — checked: no change needed - 0.6.0 ships all consumers atomically; markdown-core 0.3.0 is a MINOR (additive) bump
- **subsys.mutations** (status: active) — checked: bumped to 0.4.0 in the same close-out; consumes tables + lesson_state
- **subsys.rituals** (status: active) — checked: bumped to 0.5.0; consumes lesson_state in gauge and roll-ups
- **subsys.scaffold** (status: active) — checked: bumped to 0.3.0; doctor identity checks shipped

### Transitive dependents

- **feat.init-cli** — flagged via upstream; checked: no change needed - CLI surface unchanged beyond doctor's two new checks
- **subsys.mcp-server** — flagged via upstream; checked: bumped to 0.4.0; 24 tools

## Updates applied

Release close-out: six subsystem MINOR bumps recorded via cz_upsert_entity; engine 0.6.0; no semver pin violations (all pins satisfied by minor bumps).

## Updates deferred

PyPI publish of 0.6.0 is an open thread (uvx-by-name resolution remains stale for non-venv adopters until published).
