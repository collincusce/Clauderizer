# Cascade Report: subsys.markdown-core status active -> active

> Generated: 2026-06-09T20:58:04Z

## Trigger

`subsys.markdown-core` — status active -> active

## Affected entities

### Direct dependents

- **subsys.graph** (status: active) — checked: no change needed - graph untouched in Phase 2
- **subsys.mutations** (status: active) — checked: updated this phase - add_output, add_phase_summary, _refresh_tracker_headers (additive)
- **subsys.rituals** (status: active) — checked: updated this phase - status_bundle gains the handoff_note key (additive)
- **subsys.scaffold** (status: completed) — checked: updated this phase - doctor identity checks + stanza CLI-fallback line

### Transitive dependents

- **feat.init-cli** — flagged via upstream; checked: no change needed - init flow unchanged; the stanza refresh rides the normal marker merge
- **subsys.mcp-server** — flagged via upstream; checked: updated this phase - cz_add_output and cz_add_phase_summary registered (tools 24)

## Updates applied

Phase 2 commit: writer.set_blockquote_field + sections.is_placeholder alias in markdown-core; consumers (mutations, rituals, scaffold, mcp-server) updated atomically in the same commit.

## Updates deferred

subsys.* version bumps land with the 0.6.0 release in Phase 3.
