# Cascade Report: subsys.markdown-core status completed -> active

> Generated: 2026-06-09T20:47:52Z

## Trigger

`subsys.markdown-core` — status completed -> active

## Affected entities

### Direct dependents

- **subsys.graph** (status: completed) — checked: no change needed - graph reads frontmatter only; tables/numbering changes don't touch it
- **subsys.mutations** (status: completed) — checked: updated in the same commit - add_phase/_set_phase_row/add_lesson now route through markdown.tables (additive, no API break)
- **subsys.rituals** (status: completed) — checked: updated in the same commit - preflight profile commands run in the engine interpreter's environment (A-001)
- **subsys.scaffold** (status: completed) — checked: no change needed - templates untouched in Phase 0; init benefits passively from anchored numbering

### Transitive dependents

- **feat.init-cli** — flagged via upstream; checked: no change needed - CLI surface unchanged
- **subsys.mcp-server** — flagged via upstream; checked: no change needed - tool signatures and return shapes unchanged in Phase 0

## Updates applied

markdown-core gained markdown/tables.py (structural table-block writes) and entry-anchored next_numbered_id; mutations and rituals consume them in the same Phase 0 commit, so all dependents were updated atomically.

## Updates deferred

subsys.* version bumps deferred to the 0.6.0 release close-out (Phase 3); no semver pins affected.
