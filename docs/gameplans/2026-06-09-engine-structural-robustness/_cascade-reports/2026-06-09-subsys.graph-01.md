# Cascade Report: subsys.graph status completed -> active

> Generated: 2026-06-09T20:52:20Z

## Trigger

`subsys.graph` — status completed -> active

## Affected entities

### Direct dependents

- **subsys.mcp-server** (status: completed) — checked: no change needed - cz_cascade passes the same args to run(); result shape unchanged
- **subsys.mutations** (status: active) — checked: no change needed - resolve_cascade consumes the pending list whose shape is unchanged (order is now chronological, which is what its 'latest' default always assumed)
- **subsys.rituals** (status: active) — checked: updated in the same commit - status_bundle.pending_cascades orders by report_sort_key (date, sequence)

### Transitive dependents

- _(none)_

## Updates applied

report_filename gained an optional reports_dir param (additive); run() supplies it. status_bundle gained report_sort_key. All consumers updated atomically in the Phase 1 commit.

## Updates deferred

None.
