# Cascade Report: subsys.rituals additive change (P4): new merge_audit ritual (compute/describe) surfacing through status_bundle digest, preflight warn and cz_audit; preflight gains the merge_integrity warn check; audit gains the merge_integrity list; no existing check semantics changed, no tool added

> Generated: 2026-07-28T04:57:11Z

## Trigger

`subsys.rituals` — additive change (P4): new merge_audit ritual (compute/describe) surfacing through status_bundle digest, preflight warn and cz_audit; preflight gains the merge_integrity warn check; audit gains the merge_integrity list; no existing check semantics changed, no tool added

## Affected entities

### Direct dependents

- **feat.approval-gates** (status: completed) — checked: no change needed — consumes no ritual output the audit touched
- **feat.corpus-modernization** (status: completed) — checked: no change needed — modernize reads gitignore/procedure state, not audit signals
- **feat.deliverable-matrix** (status: completed) — checked: no change needed — deliverable board unrelated to audit surfaces
- **feat.dream-loop** (status: active) — checked: no change needed — dream gauge/digest lines untouched; merge-audit line is conditional and disjoint
- **feat.onboarding** (status: completed) — checked: no change needed — onboarding bundle reads docs presence, not audit signals
- **feat.scoped-memory** (status: completed) — checked: no change needed — audience filtering unrelated
- **feat.standing-conditions** (status: completed) — checked: no change needed — conditions evaluate via preflight/loop_step paths whose semantics are unchanged; the new merge_integrity check is warn-only and disjoint
- **subsys.hook** (status: active) — checked: no change needed — the hook renders the digest whose merge-audit line is conditional-emission (zero bytes healthy, pinned) and the compute cost is O(1) bounded (pinned <=8 subprocess calls); INVARIANT-06 posture untouched
- **subsys.mcp-server** (status: active) — checked: no change needed — additive result keys under contract 1.1; transport parity matrix green including cz_status
- **subsys.ops** (status: active) — checked: updated — cz_audit docstring now names the merge-integrity signal family (with the squash caveat) so the MCP tool description matches the result shape

### Transitive dependents

- **subsys.cli** — flagged via upstream; checked: no change needed — transitively covered; run_op path exercised by the parity matrix

## Updates applied

cz_audit docstring gained the merge-integrity family with the squash caveat (ops.py); everything else verified additive/conditional with tests already pinning the healthy-repo byte-identity

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
