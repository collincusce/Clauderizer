# Cascade Report: subsys.ops additive change (D-073/D-074): _receipted REGISTRY seam beside the refusal journal; curate/loop_step/mine_failures gain id+ledger filtering with suppressed_count/all_proposals keys; add_correction result gains possibly_contradicted advisory; no tool added or removed, no signature changed, contract 1.1 additive

> Generated: 2026-07-28T03:23:21Z

## Trigger

`subsys.ops` — additive change (D-073/D-074): _receipted REGISTRY seam beside the refusal journal; curate/loop_step/mine_failures gain id+ledger filtering with suppressed_count/all_proposals keys; add_correction result gains possibly_contradicted advisory; no tool added or removed, no signature changed, contract 1.1 additive

## Affected entities

### Direct dependents

- **subsys.cli** (status: active) — checked: no change needed — the CLI reaches every op through run_op → REGISTRY, which is exactly the seam the receipts wrapper and the ledger filtering live at, so `clauderize ops` batches and the status digest get the identical behavior with zero CLI edits; new result keys (suppressed_count, all_proposals, refusal_events, possibly_contradicted, converged_with_suppression) are additive under contract 1.1 and the CLI enumerates no result keys

### Transitive dependents

- _(none)_

## Updates applied

none — verified the REGISTRY-seam placement covers the CLI transport by construction (the same property test_read_only_ops and the miner/curator tests exercise through run_op)

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
