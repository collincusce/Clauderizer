# Cascade Report: subsys.scaffold kimi-desktop wiring is now host-topology-aware, self-healing, and handshake-verified (D-055): server_entry composes a Windows-native clauderizer-mcp.exe (never bare uvx on Windows), wire() has an 'unregistrable' status, init/doctor/status self-heal, doctor smoke-tests the command via MCP initialize, WSL-repo guidance refined. Backward-compatible: macOS/Linux uvx path unchanged, opt-out honored.

> Generated: 2026-07-18T01:43:03Z

## Trigger

`subsys.scaffold` — kimi-desktop wiring is now host-topology-aware, self-healing, and handshake-verified (D-055): server_entry composes a Windows-native clauderizer-mcp.exe (never bare uvx on Windows), wire() has an 'unregistrable' status, init/doctor/status self-heal, doctor smoke-tests the command via MCP initialize, WSL-repo guidance refined. Backward-compatible: macOS/Linux uvx path unchanged, opt-out honored.

## Affected entities

### Direct dependents

- **feat.corpus-modernization** (status: completed) — checked: no change needed — unrelated to kimi-desktop wiring
- **feat.init-cli** (status: completed) — checked: no change needed — init/doctor gained additive kimi-desktop behavior (topology-aware .exe, self-heal, handshake); existing per-host wiring and the macOS/Linux path are unchanged, opt-out honored
- **feat.onboarding** (status: completed) — checked: no change needed — unrelated to kimi-desktop wiring

### Transitive dependents

- _(none)_

## Updates applied

none — the kimi-desktop wiring changes are additive and backward-compatible (macOS/Linux uvx path byte-identical, CLAUDERIZER_NO_KIMI_DESKTOP opt-out intact, all other hosts untouched), so no dependent feature needed changes.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
