# Cascade Report: subsys.mcp-server Added `clauderizer-mcp --repo <path>` / `$CLAUDERIZER_REPO` (repo discovery decoupled from cwd, repo_ctx honors it) and the doctor MCP initialize-handshake smoke-test surface (kimidesktop.handshake_probe). Backward-compatible: cwd discovery unchanged when the override is unset.

> Generated: 2026-07-18T01:42:57Z

## Trigger

`subsys.mcp-server` — Added `clauderizer-mcp --repo <path>` / `$CLAUDERIZER_REPO` (repo discovery decoupled from cwd, repo_ctx honors it) and the doctor MCP initialize-handshake smoke-test surface (kimidesktop.handshake_probe). Backward-compatible: cwd discovery unchanged when the override is unset.

## Affected entities

### Direct dependents

- **feat.corpus-modernization** (status: completed) — checked: no change needed — unrelated to repo discovery / handshake; --repo is additive and cwd behavior is unchanged when unset
- **feat.onboarding** (status: completed) — checked: no change needed — onboarding reads docs from the resolved repo; --repo only changes HOW the repo is resolved, not what onboarding does

### Transitive dependents

- _(none)_

## Updates applied

none — the --repo/CLAUDERIZER_REPO surface and the doctor handshake are purely additive; existing cwd-based discovery is byte-identical when the override is unset, so no dependent needed changes.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
