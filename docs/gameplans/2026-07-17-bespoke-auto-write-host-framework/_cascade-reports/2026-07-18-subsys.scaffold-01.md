# Cascade Report: subsys.scaffold Bespoke auto-write host wiring is now generic over a BespokeHost registry (D-056): init/doctor/status/uninstall iterate bespoke_hosts.all_hosts() instead of hardcoding kimidesktop; kimi-desktop is the first BespokeHost impl; doctor --deep opt-in handshakes emitter hosts. Behavior-preserving for the single registered host.

> Generated: 2026-07-18T02:52:48Z

## Trigger

`subsys.scaffold` — Bespoke auto-write host wiring is now generic over a BespokeHost registry (D-056): init/doctor/status/uninstall iterate bespoke_hosts.all_hosts() instead of hardcoding kimidesktop; kimi-desktop is the first BespokeHost impl; doctor --deep opt-in handshakes emitter hosts. Behavior-preserving for the single registered host.

## Affected entities

### Direct dependents

- **feat.corpus-modernization** (status: completed) — checked: no change needed — unrelated to host wiring
- **feat.init-cli** (status: completed) — checked: no change needed — init/doctor/status/uninstall now iterate the bespoke-host registry generically, but behavior for the single registered host (kimi-desktop) is byte-identical (verified live); all other hosts untouched
- **feat.onboarding** (status: completed) — checked: no change needed — unrelated to host wiring

### Transitive dependents

- _(none)_

## Updates applied

none — the registry generalization is behavior-preserving (live doctor/status byte-identical, opt-out intact, Claude Code wiring untouched); no dependent feature needed changes.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
