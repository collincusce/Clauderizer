# Cascade Report: subsys.scaffold version 0.7.0 -> 0.8.0 (P8: host_target routing in init, new scaffold/uninstall.py, hosttargets.emit_host_wiring; additive, no API removed)

> Generated: 2026-06-21T11:26:45Z

## Trigger

`subsys.scaffold` — version 0.7.0 -> 0.8.0 (P8: host_target routing in init, new scaffold/uninstall.py, hosttargets.emit_host_wiring; additive, no API removed)

## Affected entities

### Direct dependents

- **feat.init-cli** (status: completed) — checked: updated in lockstep — feat.init-cli 0.3.0 -> 0.4.0 IS the consumer of the new scaffold capability (the --host flag drives hosttargets.emit_host_wiring). Pin ^0.5.0 still resolves to scaffold 0.8.0 (0.x-compatible, no pin_violation). Additive change, no removed API; no further action.

### Transitive dependents

- _(none)_

## Updates applied

feat.init-cli bumped to 0.4.0 (clauderize init --host flag); both entity docs reflect P8.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
