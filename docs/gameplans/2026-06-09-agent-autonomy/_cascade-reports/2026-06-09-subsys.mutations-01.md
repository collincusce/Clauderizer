# Cascade Report: subsys.mutations Phase 0: advisory write lock — all 18 public mutations serialize on .clauderizer/write.lock via locking.py; interface unchanged

> Generated: 2026-06-09T22:22:31Z

## Trigger

`subsys.mutations` — Phase 0: advisory write lock — all 18 public mutations serialize on .clauderizer/write.lock via locking.py; interface unchanged

## Affected entities

### Direct dependents

- **subsys.mcp-server** (status: active) — checked: no change needed — dispatch and tool schemas untouched; the lock is acquired inside mutations.* so every MCP writer inherits it transparently on next server start (running servers hold the pre-lock module until restart)

### Transitive dependents

- _(none)_

## Updates applied

None beyond Phase 0's own code: src/clauderizer/locking.py (new), @_locked on all 18 public mutations.* functions, RepoPaths.write_lock_file property, tests/test_locking.py (9 tests). No MCP-server source change; tool count remains 24.

## Updates deferred

Unlocked write residue outside mutations.*: cascade.run invoked directly by the cz_cascade tool, and ritual writes (preflight baseline refresh, handoff regeneration) do not acquire the lock yet — Phase 1's shared ops registry is the natural place to wrap every write op uniformly.
