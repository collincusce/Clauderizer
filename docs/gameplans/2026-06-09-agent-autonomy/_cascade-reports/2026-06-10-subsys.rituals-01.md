# Cascade Report: subsys.rituals Phase 1: preflight baseline refresh serializes on the write lock (skips self-healingly under contention); procedure shipped at 1.2.0 naming clauderize ops the fallback

> Generated: 2026-06-10T02:21:15Z

## Trigger

`subsys.rituals` — Phase 1: preflight baseline refresh serializes on the write lock (skips self-healingly under contention); procedure shipped at 1.2.0 naming clauderize ops the fallback

## Affected entities

### Direct dependents

- **subsys.mcp-server** (status: active) — checked: no change needed — cz_preflight's signature and result shape are unchanged; the lock lives inside the baseline write site (_write_back_baseline) and contention degrades to a skipped refresh that self-heals on the next green run

### Transitive dependents

- _(none)_

## Updates applied

rituals/preflight.py: the one tracked write (baseline refresh) acquires the H-05 write lock and yields under contention; GAMEPLAN-PROCEDURE.md shipped at 1.2.0 with the "Recording Without MCP" section, PROCEDURE_VERSION synced (MINOR-compatible per doctor's MAJOR-only drift check).

## Updates deferred

None.
