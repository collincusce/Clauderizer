# Cascade Report: subsys.scaffold 0.6.0 → 0.7.0: init now wires the event-dispatching hook (UserPromptSubmit + SessionStart), injects the AGENTS.md stanza, and emits the non-destructive kimi setup snippet

> Generated: 2026-06-20T00:22:45Z

## Trigger

`subsys.scaffold` — 0.6.0 → 0.7.0: init now wires the event-dispatching hook (UserPromptSubmit + SessionStart), injects the AGENTS.md stanza, and emits the non-destructive kimi setup snippet

## Affected entities

### Direct dependents

- **feat.init-cli** (status: completed) — checked: No change needed — the changes are additive (init still produces the prior layout, now also registering the UserPromptSubmit hook and writing AGENTS.md + .clauderizer/kimi-setup.md). The pin subsys.scaffold@^0.5.0 remains satisfied by 0.7.0 (graph pin_violations empty), and init's public contract/idempotency is unchanged.

### Transitive dependents

- _(none)_

## Updates applied

None required. Verified no pin violation after the 0.6.0→0.7.0 bump; feat.init-cli's behavior is a superset of before and its exit criteria (idempotent re-init, layout) still hold under the existing + new tests.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
