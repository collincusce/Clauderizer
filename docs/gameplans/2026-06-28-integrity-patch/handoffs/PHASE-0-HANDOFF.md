<!-- clauderizer:handoff:start -->
# Phase 0 Handoff

> For: next Clauderizer session
> Gameplan: 2026-06-28-integrity-patch
> Generated by: cz_write_handoff

## What This Phase Does

_(see GAMEPLAN.md for the phase definition)_

## Pre-Flight Verification (MANDATORY)

Run `cz_preflight` before touching code. All enabled checks must pass.

## Key Files You Must Read

- `docs/gameplans/2026-06-28-integrity-patch/GAMEPLAN.md`
- `docs/gameplans/2026-06-28-integrity-patch/PHASE-STATUS.md`
- `CLAUDE.md`

## Governing Invariants for This Phase

_(4 of 8 surfaced — the must-hold rules whose text overlaps this phase (keyword + entity-id, no ML). Honor these; the full set is in `docs/INVARIANTS.md`.)_

- **INVARIANT-05** — Discipline gates (clarify/open-items, exit-criteria, analyze-against-invariants) are advisory and judgment-based: they surface findings in tool results for the agent to act on, and MUST NOT hard-block a mutation or phase transition, nor introduce an enable/disable config flag. The engine surfaces candidates; the agent decides.
- **INVARIANT-01** — Markdown is canonical — the index is rebuilt from it on disagreement.
- **INVARIANT-04** — The SessionStart hook never blocks a session — it always exits 0.
- **INVARIANT-06** — Every hook event handler is read-only and always exits 0 — a memory tool never mutates docs or blocks a session from inside a hook. Generalizes INVARIANT-04 from SessionStart to all dispatched events (SessionStart, UserPromptSubmit, PreCompact, PostCompact, ...).

## Skills for This Phase

_(Agent Skills registered for this project whose trigger overlaps this phase — invoke the relevant ones. The full inventory is in `docs/SKILLS.md`; discover more with cz_discover_skills, register with cz_register_skill.)_

- **clauderizer-new-gameplan** — Plan a new multi-phase gameplan from a goal. Use when the user wants to start a new initiative, project, or large featu…
- **clauderizer-amend** — Change a gameplan after it has started executing. Use when scope shifts mid-flight — a phase needs a task it's missing,…
- **clauderizer-cascade** — After changing a tracked entity (subsystem, feature, decision, invariant, or a status/version), walk its dependents and…
- **clauderizer-record** — Quickly capture a decision, invariant, lesson, correction, or risk into the right place. Use when the user says "rememb…

## Accumulated Lessons (Cumulative — All Prior Phases)

_(Numbered sequentially across the whole gameplan. Categorized. Pruned of
obsolete items — mark with "(obsolete)" rather than deleting.)_

### Category: Process

_(none yet)_

## Project Lessons (most relevant to this phase)

_(5 of 30 shown — ranked by keyword + entity-id overlap with this phase, no ML. The full set is canonical in `docs/LESSONS.md`; the handoff focuses under memory pressure without dropping anything from canonical memory.)_

**L-39.** A deterministic relevance/gain kill-gate concludes only what its fixtures permit. When the eval corpus is cleanly separable (disjoint topics — as tests/benchmarks RANKER_ENTRIES is), the baseline saturates at the 1.0 recall/nDCG/MRR ceiling, so NO scoring change can show lift: the gate proves "no regression", never "no value", and can only return DISCARD/break-even by construction. To FALSIFY "richer scorer X helps", build the adversarial fixture targeting X's mechanism FIRST — for BM25/Okapi: length-bias confounds, term-frequency skew, near-duplicate topics. Extends L-32 (saturated target ⇒ predict the null) with a fixture-design corollary. Measured instance (2026-06-24-ranker-spike): stdlib BM25 (k1=1.5, b=0.75) tied the raw-overlap baseline at 1.0000 on every extraction+multi_session metric and across a full b-sweep, with guards flat (multi_session precision 0.75, knowledge_updates contradiction 0.0) ⇒ a clean measured DISCARD. Watch-out for any future ranker swap: BM25 length-normalization breaks the stale-vs-current SECONDARY-sort tie that supersession-demotion (analyze.rank_relevant) relies on, so it needs a length-adversarial knowledge_updates fixture before it can be called safe. *(from 2026-06-24-ranker-spike)*
**L-40.** When you author BOTH an eval fixture and the detector it scores, a 100%-detection / 0-false-positive result is suspicious by construction — you may be teaching to the test (the confirmation-bias face of L-32/L-39). Give the fixture discriminating power: seed adversarial NEAR-MISSES (a long-but-hollow note; a paper citation that is ALSO in-repo-anchored; a factual "all criteria met" with no intensifier) and run a deliberately NAIVE strawman detector beside the real one. The result is credible only if the principled detector beats the strawman ON the near-misses (a real mechanism), not merely beats a prior no-check baseline (a tautology — anything beats zero). Extends L-39: don't just build the adversarial fixture first — build a strawman the fixture must DEFEAT. Measured: 2026-06-24-critique-bias-hardening, principled 100% det / 0% FP vs naive 20-50% FP; naive wrong on 6/7 near-misses, principled on 0. *(from 2026-06-24-critique-bias-hardening)*
**L-43.** A parked feature branch that has fallen behind a newer release reconciles cleanly when the two feature sets are orthogonal in code: dry-run the merge (git merge --no-commit main) to MEASURE the real conflict surface before assuming a heavy rewrite. Here main(1.2.0) auto-merged into the abstract-index branch with a single config-pointer conflict; the union suite went green with zero manual code fixes. Complete parked work by reconciling with the release line FIRST, then finishing the remaining phases against the real target. *(evidence: abstract-index Phase 6/7 resume 2026-06-28: merged main into feat/abstract-index-fast-retrieval, 1 conflict (.clauderizer/config.toml), union suite 711, PR #16 9-cell green)* *(from 2026-06-25-abstract-index-fast-retrieval)*
**L-42.** release-check's clean_tree counts UNTRACKED files (foreign tool artifacts, regenerable caches) as a dirty tree and blocks the ritual, even though the published artifact builds from origin/main + the tag (which never contain them). The surgical, honest fix is .git/info/exclude (local-only, non-committed, deletes nothing) for files that aren't this repo's content — not deleting another tool's files and not committing junk. Verify `git status --untracked-files=no` is empty first to prove no real source change is uncommitted. *(evidence: 1.2.0 release: 8 untracked (.agents/, higgsfield skills, skills-lock.json, abstract_index.json, fixture index.json) -> .git/info/exclude -> release-check exit 0)* *(from 2026-06-27-concurrent-multi-axis-gameplans)*
**L-44.** For near-DUPLICATE detection, length-normalize the token overlap (Jaccard = |A∩B|/|A∪B|) — never the raw overlap COUNT (the relevance-ranking signal), which a long distinct-but-similar entry trips by sheer size. Same tokenizer, different normalization for a different question (is it RELEVANT vs is it the SAME). Build the measuring stick first: a labeled fixture + a naive count strawman the principled detector must beat ON adversarial near-misses (L-40), not merely a no-check baseline. Measured (abstract-index Phase 5): Jaccard@0.40 precision 1.0 / recall 1.0, 0 false-positives on 2 near-misses the count strawman flagged. *(from 2026-06-25-abstract-index-fast-retrieval)*

## Ending Protocol

1. `cz_transition_phase` the finished phase to complete.
2. `cz_add_output` each concrete produced value; `cz_add_phase_summary` the recap; `cz_add_correction` / `cz_add_lesson` as earned.
3. `cz_transition_status` on touched entities (fires cascade); `cz_resolve_cascade` the verdicts.
4. `cz_write_handoff` for the next phase.
5. Run exit verification; report the test count.
<!-- clauderizer:handoff:end -->

## Phase Notes (agent-owned)

> **You are a fresh session.** This block + `GAMEPLAN.md` are self-contained.
> This is a **PATCH / hardening** gameplan (no new features, no user-facing
> behavior regression → ships as **1.3.1**). It fixes the 6 findings from a
> read-only integrity audit done at 1.3.0. The codebase is structurally sound;
> these are coherence/credibility/doc-drift fixes, not a rescue.

### Provenance & source-of-truth values (do not invent — verify, then record)
- **Branch:** `fix/integrity-patch`, off `main` at **1.3.0** (the plan commit is the
  branch's first commit). Lean process (per the user): sync with main only at
  **closeout**, not per-phase — this is a single solo feature.
- **Baseline suite:** expect **711 passed / 4 skipped** (the union suite that
  shipped 1.3.0, PR #16). Phase 0 must run `cz_preflight` and **record the real
  number** via `cz_add_output` — don't trust this note, measure.
- **Version:** `1.3.0` in both `pyproject.toml:8` and `src/clauderizer/__init__.py:7`
  → bump to **1.3.1** in Phase 5. `PROCEDURE_VERSION` stays `1.4.0`.
- **Corpus:** **30 active project lessons** in `docs/LESSONS.md` (the bloat the
  curator-loop gameplan will later consolidate — NOT this gameplan's job; we only
  make the redundancy metric trustworthy so that work can proceed).
- **Tool surface:** **42** `cz_*` tools (welded by `tests/test_ops.py` parity gate).

### The keystone (Phase 1) — exact targets. SEE CORRECTION C-01.
There is **no `corpus_health.py`** — all redundancy logic is in
**`src/clauderizer/telemetry.py`**:

| What | Where | Detail |
|---|---|---|
| Threshold | `telemetry.py:36` | `_REDUNDANCY_THRESHOLD = 0.6` |
| **The fork** (remove) | `telemetry.py:108-110` | `{t for t in re.findall(r"[a-z0-9]+", text.lower()) if len(t) > 2}` — keeps 3-char words, **keeps all stopwords**, len>2 |
| `_jaccard` | `telemetry.py:113-116` | fine as-is, keep |
| Call site A | `telemetry.py:149,153` | `corpus_health()` → `redundant_pairs` (feeds `cz_corpus_health`) |
| Call site B | `telemetry.py:327,331` | `curate_proposals()` → consolidate (feeds `cz_curate`; `lesson_health`/`cz_loop_step` reach it transitively) |

The **canonical** tokenizer is `analyze._tokens` (`src/clauderizer/analyze.py:43-48`):
drops `_STOP`, keeps a token only if `len>=4 OR has-digit`. Phase-1 edit:
`from .analyze import _tokens` in telemetry.py (delete the local def), so both
sites tokenize canonically.

**Why measure before changing (fixture-first, L-39/L-40):** the direction of the
bias is NOT obvious a priori. The fork keeps shared stopwords (which inflate
`A∩B` → push Jaccard up) AND keeps distinct 3-char/stopword noise (which inflates
`A∪B` → push Jaccard down). The audit's *observed* symptom is `cz_corpus_health`
reporting **0 redundant pairs** despite visible near-dupes in the 30 lessons,
which points to **under**-counting in practice — but Phase 0 must **prove** it:
record the redundant-pair count under the fork vs. under `analyze._tokens` over
the same `docs/LESSONS.md`, at threshold 0.6. That before/after pair is the
gain measurement and it drives **O-01** (keep 0.6 or recalibrate — by data, not taste).

**Guard test (Phase 1):** after the fix the only `def _tokens` in `src/clauderizer`
is `analyze.py:43`. Assert it behaviorally — e.g. `telemetry._tokens is
analyze._tokens` (import identity) — and/or a structural check that `def _tokens`
appears exactly once under `src/`. That's what stops a third fork reappearing
(this is the enforcement that makes **O-04** — promote D-041 to INVARIANT-09 — viable).

### The other five findings (P2–P4) — exact targets
- **#5 (P2)** dup lesson-line regex: `rituals/handoff.py:155` `_PROJECT_LESSON_NUM_RE`
  is a copy of `graph/abstract_index.py:69` `_LESSON_LINE_RE`. Single-source it
  (reuse `abstract_index.parse_lesson_line` / one shared regex) + a test the two
  agree. NB: a *third* identical copy exists at `telemetry.py:127` (`num_re` in
  `_active_project_lessons`) — fold that in too while you're here.
- **#6a (P2)** `kinds/campaign.toml:22` declares preflight gates
  (virality/brand_lint/duration) with no shipped wiring → campaign gameplans
  read false-green. Decide loud-warn vs. ship example `.clauderizer/preflight.campaign.toml`
  (**O-02**) + test.
- **#6b (P2)** `analyze.suggest_edges` is O(n²) over all entities and runs on the
  hot `UserPromptSubmit` hook path (via `cz_analyze`) — add a size guard/cap
  before the pair loop (mind **INVARIANT-06**: hooks stay cheap & read-only) + test.
- **#6c (P2)** `cz_get` is `writes=False` but `get_entry → abstract_index.load_or_rebuild`
  may write the **gitignored disposable cache** (same as `cz_graph_query`,
  INVARIANT-01). Add a one-line clarifying comment + a test asserting `cz_get`
  mutates **no tracked markdown**.
- **#3 (P3)** kill/replace ~5 tautological `writes is False` + `__name__` tests
  (`test_lesson_health.py:86`, `test_loop_gameplan.py:101`, `test_curator.py:75`,
  `test_skill_discovery.py:105`, `test_analyze.py:273`) with behavioral read-only
  assertions (the `test_hook_dispatch` snapshot-no-tracked-change pattern) — or
  drop the redundant ones, keeping the real `test_ops.py` parity + signature-drift
  gate. Fix `test_mcp_tools.py:38` (stale 16-of-42 subset → full surface or fold
  in). Fix stale comment `test_ops.py:56` (`24/24` → 42). **ADD** two real tests:
  (a) SessionStart digest's advertised tool list == `TOOL_NAMES` (**O-03**: find
  the render seam first); (b) per-kind preflight command-gate via a **real**
  subprocess (current `test_per_kind_preflight.py` uses a fake runner).
- **#4 (P3, PII)** `test_diverse_robustness.py:262` hardcodes a
  `/mnt/c/Users/<username>/.claude/...` literal — a **username (PII)** and
  machine-specific (the real string is in the test file at that line).
  Scrub it; make the skip portable (env var or skip-if-absent, no home literal).
  Per the no-PII rule: grep for the username across the repo before the P5 commit.
- **#2 (P4, docs)** `docs/ARCHITECTURE.md` (~0.15.0) + `docs/VISION.md` (pre-1.2.0)
  never mention 1.2.0 (concurrent/multi-axis gameplans, kinds, cz_focus/gameplans/
  consumes, cross-gameplan deps) or 1.3.0 (abstract index, cz_get, abstracts on
  cz_analyze, dedup advisory). Bump stale `docs/subsystems/mcp-server.md`
  frontmatter `version: 0.5.0`. Add `cz_resolve_finding` to the README mutations
  prose (its pair `cz_add_finding` is listed). Human-first prose (D-038/D-039);
  leave the single-sourced CLAUDE.md/AGENTS.md stanza (L-16) alone. Docs-only.

### Decisions already on record (don't re-litigate)
- **D-041** (project): one canonical tokenizer (`analyze._tokens`) for all
  lexical-overlap/similarity. **D1** (gameplan): tests assert runtime behavior,
  not module-load constants. **D2** (gameplan): patch not minor — no behavior
  regression, suite stays green, targets 1.3.1.
- Open items: **O-01** threshold recalibration · **O-02** campaign false-green
  fix-choice · **O-03** digest-advertisement test seam · **O-04** promote D-041
  to INVARIANT-09. Resolve each with `cz_resolve_open_item` as you close its phase.

### Governing rules to honor
INVARIANT-01 (markdown canonical; the cache is disposable) · INVARIANT-05
(advisory never blocks, no on/off flag) · INVARIANT-06 (hooks read-only, exit 0,
cheap) · INVARIANT-07 (the release is handed to the user — don't auto-publish
1.3.1 without the go). D-011 release ritual + D-012 9-cell CI for Phase 5;
L-19 (refresh editable install after a version bump), L-20 (run every CI host
leg before the irreversible publish; push-then-release ordering).

### Downstream unblock (flag at close, don't do it here)
The `2026-06-21-standing-curator-loop` gameplan's 30-lesson consolidation is
**blocked on this gameplan**: it needs a trustworthy `cz_corpus_health`, which is
exactly what Phase 1 delivers. Note the unblock in the Phase 5 post-mortem.
