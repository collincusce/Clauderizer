# Cascade Report: subsys.scaffold Opt-in WSL-serving pin (D-057): init gained --serve-wsl-here (writes a durable clauderizer-serve.json sidecar + composes {--repo UNC, cwd win-safe}); self-heal composes the pin from the sidecar (survives app-wipe); doctor reports the pinned repo + tradeoff; uninstall clears the sidecar. Strictly opt-in; default repo-agnostic behavior unchanged.

> Generated: 2026-07-18T09:02:49Z

## Trigger

`subsys.scaffold` — Opt-in WSL-serving pin (D-057): init gained --serve-wsl-here (writes a durable clauderizer-serve.json sidecar + composes {--repo UNC, cwd win-safe}); self-heal composes the pin from the sidecar (survives app-wipe); doctor reports the pinned repo + tradeoff; uninstall clears the sidecar. Strictly opt-in; default repo-agnostic behavior unchanged.

## Affected entities

### Direct dependents

- **feat.corpus-modernization** (status: completed) — checked: no change needed — unrelated to the pin
- **feat.init-cli** (status: completed) — checked: no change needed — init gained an additive --serve-wsl-here flag; default init behavior and all other hosts are unchanged (opt-in, off-combo no-op, verified live)
- **feat.onboarding** (status: completed) — checked: no change needed — unrelated to the pin

### Transitive dependents

- _(none)_

## Updates applied

none — the pin is strictly opt-in and additive (default repo-agnostic behavior byte-identical; live-verified); no dependent feature needed changes.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
