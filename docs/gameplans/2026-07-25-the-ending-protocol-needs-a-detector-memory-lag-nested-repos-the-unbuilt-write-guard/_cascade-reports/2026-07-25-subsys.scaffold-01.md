# Cascade Report: subsys.scaffold 0.15.0 -> 0.16.0: nested-install ownership (H-23) — nesting.py silences a non-owner install per hook event; init warns under a clauderized ancestor; doctor names nested installs and the ancestor

> Generated: 2026-07-25T21:45:58Z

## Trigger

`subsys.scaffold` — 0.15.0 -> 0.16.0: nested-install ownership (H-23) — nesting.py silences a non-owner install per hook event; init warns under a clauderized ancestor; doctor names nested installs and the ancestor

## Affected entities

### Direct dependents

- **feat.corpus-modernization** (status: completed) — checked: no change needed — modernize reads the corpus and config stamp; it does not consult repo nesting, and no proposal shape changed
- **feat.dream-loop** (status: active) — checked: no change needed — the dream journal and proposal ledger are per-repo under .clauderizer/ and are reached only by the owning install, which is exactly the behavior ownership preserves
- **feat.init-cli** (status: completed) — checked: UPDATED — docs/features/init-cli.md gained a "Nested installs warn, they do not refuse" paragraph under the idempotency section (init's user-visible surface changed: a new `!` warning naming the clauderized ancestor), and the entity went 0.4.0 -> 0.5.0. Exit codes are unchanged — nesting warns and proceeds, it never refuses.
- **feat.onboarding** (status: completed) — checked: no change needed — onboarding seeds corpus docs after init; it neither reads the hook payload nor scans for nested installs

### Transitive dependents

- _(none)_

## Updates applied

Updated docs/features/init-cli.md (nested-install warning paragraph; 0.4.0 -> 0.5.0). Updated docs/subsystems/scaffold.md with a "Nested installs (H-23)" section covering the ownership model, its deliberately narrow scope, and the doctor/init surfaces; 0.15.0 -> 0.16.0. No dependent pin required a change — subsys.scaffold's minor bump is compatible with every ^0.5.0-style dependency.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
