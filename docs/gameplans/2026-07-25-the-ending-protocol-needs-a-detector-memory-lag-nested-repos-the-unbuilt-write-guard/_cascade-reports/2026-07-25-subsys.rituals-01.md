# Cascade Report: subsys.rituals 0.11.0 -> 0.12.0: added memory_lag.py (H-22/D-069) — a git-derived, advisory memory-lag detector wired into status_bundle.compute/render_digest and preflight.run as a conditionally-appended warn

> Generated: 2026-07-25T21:37:05Z

## Trigger

`subsys.rituals` — 0.11.0 -> 0.12.0: added memory_lag.py (H-22/D-069) — a git-derived, advisory memory-lag detector wired into status_bundle.compute/render_digest and preflight.run as a conditionally-appended warn

## Affected entities

### Direct dependents

- **feat.approval-gates** (status: completed) — checked: no change needed — the approval_gates pre-flight check is untouched; memory_lag is appended after it and both are conditionally-present warns that do not interact
- **feat.corpus-modernization** (status: completed) — checked: no change needed — the ⚙ modernization digest line is unaffected; memory_lag adds a separate conditionally-emitted line below it
- **feat.deliverable-matrix** (status: completed) — checked: no change needed — the deliverables rollup reads bundle['deliverables'] only; the new bundle['memory_lag'] key is additive and never rendered by the matrix path
- **feat.dream-loop** (status: active) — checked: no change needed — the dream gauge and 🌙 plea lines are unaffected; dreams.jsonl is under .clauderizer/ which memory_lag excludes from its work-commit count by construction
- **feat.onboarding** (status: completed) — checked: no change needed — onboarding writes corpus docs and does not read the status bundle or the pre-flight check list
- **feat.scoped-memory** (status: completed) — checked: no change needed — handoff assembly and lesson propagation are untouched; the detector reads git and the phase tracker, never the lesson corpus
- **feat.standing-conditions** (status: completed) — checked: no change needed — standing_conditions remains the last kind-driven check; memory_lag appends after it and, unlike conditions, runs fixed bounded git reads rather than user shell probes, so the hook-safety rationale that gates conditions behind compute(conditions=True) does not apply
- **subsys.mcp-server** (status: active) — checked: no change needed — no tool was added or removed, so TOOL_NAMES/REGISTRY parity is unchanged. cz_status and cz_preflight stay thin wrappers and simply carry the new bundle key / conditional check through. Verified the doc's lock claim still holds: the check calls the read-only status_bundle.compute, so cz_preflight still takes the write lock only at the baseline refresh.

### Transitive dependents

- _(none)_

## Updates applied

none — the change is strictly additive. docs/subsystems/rituals.md gained a "Memory-lag detection" section and went 0.11.0 -> 0.12.0; no dependent doc or pin required an edit, and the digest/check-list byte-identity for a lag-free repo is held by tests/test_back_compat_focus.py plus tests/test_memory_lag.py::test_digest_byte_identical_when_there_is_no_lag and ::test_preflight_check_list_unchanged_when_memory_is_current.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
