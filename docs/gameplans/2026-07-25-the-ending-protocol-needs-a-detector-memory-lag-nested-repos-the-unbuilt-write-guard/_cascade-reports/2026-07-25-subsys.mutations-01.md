# Cascade Report: subsys.mutations 0.7.0 -> 0.8.0: the write guard 1.14.0 specified and did not ship — _strip_toolcall_markup neutralizes leaked tool-call framing and unbalanced closing tags at the D-066 render boundary (normalize, never reject)

> Generated: 2026-07-25T21:51:48Z

## Trigger

`subsys.mutations` — 0.7.0 -> 0.8.0: the write guard 1.14.0 specified and did not ship — _strip_toolcall_markup neutralizes leaked tool-call framing and unbalanced closing tags at the D-066 render boundary (normalize, never reject)

## Affected entities

### Direct dependents

- **feat.approval-gates** (status: completed) — checked: no change needed — approval markers are engine-rendered (cz_approve_gate composes the date/hash text); the only caller string is the optional note, which flows through the same _safe_body boundary and is unaffected unless it carries stray markup
- **feat.dream-loop** (status: active) — checked: no change needed — dream notes do not route through mutations._safe_body; dreams.validate is their own write-time schema check (D-058) and already rejects PII/shape violations. Worth noting the two guards are siblings by design and neither supersedes the other
- **feat.scoped-memory** (status: completed) — checked: no change needed — lesson/handoff propagation is unchanged; add_lesson gained the same normalization every other record write already had, and tests/test_toolcall_write_guard.py::test_add_finding_and_add_lesson_are_guarded_too pins it
- **subsys.mcp-server** (status: active) — checked: no change needed — no tool signature changed and no result key was added or removed; the cz_* wrappers are unaffected because the guard sits below them at the render boundary

### Transitive dependents

- **feat.corpus-modernization** — flagged via upstream; checked: no change needed (transitive) — modernize never writes (D-042), so it cannot reach the mutations render boundary at all
- **feat.onboarding** — flagged via upstream; checked: no change needed (transitive) — onboarding seeds corpus docs through the same blessed writes and simply inherits the stronger boundary; no onboarding-specific behavior depends on markup surviving

## Updates applied

Updated docs/subsystems/mutations.md with a new "Well-formedness at the write boundary (D-066)" section — the boundary was undocumented since 1.14.0 shipped it, so this closes that drift as well as documenting the new tool-call guard; 0.7.0 -> 0.8.0. No dependent required an edit: the guard sits BELOW every cz_* wrapper at the render boundary, so no tool signature, result key or doc surface changed. Behavior change is confined to argument values that carry leaked tool-call framing or unbalanced closing tags outside code spans.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
