# Cascade Report: subsys.graph 0.3.0 -> 0.4.0: the scan reports what it could not load — model.from_file returns a Drop record, index.build accumulates drops + duplicate-id collisions, Graph.integrity() publishes the accounting, and cz_cascade on an unknown entity returns ok:false

> Generated: 2026-07-25T21:59:58Z

## Trigger

`subsys.graph` — 0.3.0 -> 0.4.0: the scan reports what it could not load — model.from_file returns a Drop record, index.build accumulates drops + duplicate-id collisions, Graph.integrity() publishes the accounting, and cz_cascade on an unknown entity returns ok:false

## Affected entities

### Direct dependents

- **feat.deliverable-matrix** (status: completed) — checked: no change needed — deliverables_for reads entity frontmatter directly from docs/entities/*.md, bypassing the graph index entirely, so drop accounting does not change its output
- **subsys.mcp-server** (status: active) — checked: UPDATED behavior, no doc edit needed — cz_cascade now returns ok:false for an unknown entity. The doc already describes cascade as judgment-based and does not enumerate its result keys, and no tool was added or removed, so TOOL_NAMES/REGISTRY parity is untouched. Verified the change is confined to the op's guard: cascade_mod.run is unchanged, so the auto-cascade path inside transition_status (which only ever runs for an entity it just wrote) is unaffected.
- **subsys.mutations** (status: active) — checked: no change needed — transition_status calls cascade_mod.run directly, not the cz_cascade op, and only for an entity it has just written to disk; the unknown-entity guard therefore cannot fire on that path. Confirmed by tests/test_cascade.py staying green.
- **subsys.rituals** (status: active) — checked: no change needed — status_bundle._drift_warnings and _dag_integrity_warnings call index.build and iterate graph.all(); the new drops/collisions fields are additive and unread there. Deliberately NOT surfaced in the digest: INVARIANT-08 keeps injected status minimal, and doctor + cz_corpus_health are the right depth for a corpus-integrity fault.

### Transitive dependents

- **feat.approval-gates** — flagged via upstream; checked: no change needed (transitive) — approval state is computed from artifact hashes and GAMEPLAN.md criteria, with no graph dependency
- **feat.corpus-modernization** — flagged via upstream; checked: no change needed (transitive) — modernize's detectors read the corpus and config; it gains nothing and loses nothing from graph integrity reporting
- **feat.dream-loop** — flagged via upstream; checked: no change needed (transitive) — dreams.py consumes corpus_health for its distillation input and simply inherits the new graph_integrity key; nothing it reads was renamed or removed
- **feat.onboarding** — flagged via upstream; checked: no change needed (transitive) — onboarding writes entity docs through cz_upsert_entity, which produces well-formed UTF-8 frontmatter and therefore never generates a drop
- **feat.scoped-memory** — flagged via upstream; checked: no change needed (transitive) — lesson propagation and handoff assembly do not read the entity graph's integrity block
- **feat.standing-conditions** — flagged via upstream; checked: no change needed (transitive) — conditions evaluate shell probes declared per gameplan; no graph coupling

## Updates applied

Updated docs/subsystems/graph.md with a "The scan says what it could NOT load" section covering the three-way from_file return, the conservative drop classification, the drops/collisions accounting identity, and the cz_cascade payoff; 0.3.0 -> 0.4.0. No dependent doc or pin required an edit — the Graph dataclass gained fields (additive, defaulted) and no consumer's read was renamed or removed. One deliberate non-change recorded above: graph integrity is NOT surfaced in the session digest, because INVARIANT-08 keeps injected status minimal and doctor + cz_corpus_health are the right depth for it.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
