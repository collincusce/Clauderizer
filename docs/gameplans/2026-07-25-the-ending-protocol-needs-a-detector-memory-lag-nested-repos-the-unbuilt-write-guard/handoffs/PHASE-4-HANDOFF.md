<!-- clauderizer:handoff:start -->
# Phase 4 Handoff

> For: next Clauderizer session
> Gameplan: 2026-07-25-the-ending-protocol-needs-a-detector-memory-lag-nested-repos-the-unbuilt-write-guard
> Generated by: cz_write_handoff

## What This Phase Does

_(see GAMEPLAN.md for the phase definition)_

## Pre-Flight Verification (MANDATORY)

Run `cz_preflight` before touching code. All enabled checks must pass.

## Key Files You Must Read

- `docs/gameplans/2026-07-25-the-ending-protocol-needs-a-detector-memory-lag-nested-repos-the-unbuilt-write-guard/GAMEPLAN.md`
- `docs/gameplans/2026-07-25-the-ending-protocol-needs-a-detector-memory-lag-nested-repos-the-unbuilt-write-guard/PHASE-STATUS.md`
- `CLAUDE.md`

## Governing Invariants for This Phase

_(5 of 9 surfaced — the must-hold rules whose text overlaps this phase (keyword + entity-id, no ML). Honor these; the full set is in `docs/INVARIANTS.md`.)_

- **INVARIANT-09** — All lexical-overlap and similarity computations in the engine use the single canonical tokenizer analyze._tokens — there is exactly one token-splitter definition under src/, and the near-duplicate-lesson threshold is single-sourced (analyze._LESSON_DUP_JACCARD), so relevance ranking, the abstract-index token_set, the write-time near-duplicate advisory, and the corpus-health/curator redundancy metric all share one definition of "near-duplicate". Promotes D-041 from a project decision to a hard, machine-checked rule now that tests/test_canonical_tokenizer.py enforces it (exactly one `def _tokens` in src/ + import identity + threshold parity); a second fork makes the suite fail.
- **INVARIANT-05** — Discipline gates (clarify/open-items, exit-criteria, analyze-against-invariants) are advisory and judgment-based: they surface findings in tool results for the agent to act on, and MUST NOT hard-block a mutation or phase transition, nor introduce an enable/disable config flag. The engine surfaces candidates; the agent decides.
- **INVARIANT-07** — Claude Code parity never regresses: any change that degrades the current Claude Code experience (hook-driven SessionStart/UserPromptSubmit auto-injection, the cz_* tool surface, skills, or the status digest) is a release blocker. Cross-host generalization is strictly additive.
- **INVARIANT-06** — Every hook event handler is read-only and always exits 0 — a memory tool never mutates docs or blocks a session from inside a hook. Generalizes INVARIANT-04 from SessionStart to all dispatched events (SessionStart, UserPromptSubmit, PreCompact, PostCompact, ...).
- **INVARIANT-08** — Cross-host status injection reaches the model at most once per session, across all active tiers (hook, auto-resource, prompt, AGENTS.md floor, and the server-side bootstrap), deduplicated via an in-memory, read-only, session-scoped signal — never a persisted/config flag (per INVARIANT-05) and never from a path that mutates docs or blocks the session (per INVARIANT-06). Injected status stays focused and minimal (per D-027).

## Most Relevant Lessons for This Phase

_(Ranked pointers into the full list below — keyword + entity-id overlap with this phase, no ML, no drops. The cumulative list is unchanged.)_

- **#2** — "Demonstrated RED on the pre-fix tree" is satisfied by an ImportError only in the letter. A new mod…
- **#6** — A function that returns the same value for "nothing here" and "this failed" has no failure mode — i…
- **#1** — A drift detector earns its line in the digest by being QUIET, and quiet comes from gating on the st…
- **#5** — When a guard must reject a CLASS of malformed input, look for the structural property that separate…
- **#3** — When two independent installs of the same tool both fire for one session, the fix is OWNERSHIP, not…

## Skills for This Phase

_(Agent Skills registered for this project whose trigger overlaps this phase — invoke the relevant ones. The full inventory is in `docs/SKILLS.md`; discover more with cz_discover_skills, register with cz_register_skill.)_

- **clauderizer-close-gameplan** — Close out a completed (or explicitly deferred) gameplan. Use when all phases are done and the user wants to wrap up and…
- **clauderizer-new-gameplan** — Plan a new multi-phase gameplan from a goal. Use when the user wants to start a new initiative, project, or large featu…
- **clauderizer-amend** — Change a gameplan after it has started executing. Use when scope shifts mid-flight — a phase needs a task it's missing,…
- **clauderizer-cascade** — After changing a tracked entity (subsystem, feature, decision, invariant, or a status/version), walk its dependents and…
- **clauderizer-do-phase** — Execute or continue the current gameplan phase end-to-end — pre-flight, do the work, then close out (handoff + status t…

## Accumulated Lessons (Cumulative — All Prior Phases)

_(Numbered sequentially across the whole gameplan. Categorized. Pruned of
obsolete items — mark with "(obsolete)" rather than deleting.)_

### Category: Process

**1.** A drift detector earns its line in the digest by being QUIET, and quiet comes from gating on the state that asserts nothing-has-happened — not from tuning a threshold. Memory-lag fires only where the tracker claims a phase has not begun; gating instead on "in_progress phase with N commits" would fire on every normal working session, and a detector that cries wolf is worth less than none. Two corollaries. (1) Anchor the claim to the artifact's own git history — the last commit that TOUCHED the tracker — never to a date cell the writer typed, so the signal is evidence read rather than the tracker vouching for itself (D-065); this also makes "the same repo with the state recorded produces nothing" fall out for free, because recording it moves the anchor. (2) State the scope limit in the module docstring and the finding resolution instead of widening the claim to cover it: git cannot distinguish "still working" from "done but unrecorded", so a stale in_progress phase is undetectable, and saying so is stronger than a fuzzy staleness score that pretends otherwise. *(evidence: Phase 0, commit 61317f5: src/clauderizer/rituals/memory_lag.py UNSTARTED_STATES gate; tests/test_memory_lag.py::test_silent_for_an_in_progress_phase pins the quiet case, ::test_silent_once_the_tracker_records_the_work pins the anchor-moves case; H-22 resolution records the limit)*

**3.** When two independent installs of the same tool both fire for one session, the fix is OWNERSHIP, not coordination. The instinct is a shared "someone already spoke" flag — but that is cross-process persisted state, and the correct question is not "did anyone speak?" but "whose session is this?". Derive a single owner from the data every process already has (here: the nearest clauderized ancestor of the payload's session cwd), recompute it per event, and let non-owners fall silent. No flag, no file, nothing to go stale, and it works identically for two installs or ten. Two things this discipline forces: (a) thread ownership through EVERY handler, not just the loud one — the outer install would otherwise re-enter context through UserPromptSubmit after being silenced at SessionStart; and (b) scope the silence to the exact pathology (owner is a PROPER DESCENDANT of my root) rather than to "any session that isn't mine", because the broader rule quietly breaks anyone who wired a global hook deliberately. *(evidence: Phase 1, commit 439dd1b: src/clauderizer/nesting.py outranked_by; tests/test_nested_installs.py::test_every_event_honors_ownership and ::test_unrelated_session_cwd_is_unchanged pin (a) and (b); ::test_ownership_is_not_persisted pins the no-flag property)*

**5.** When a guard must reject a CLASS of malformed input, look for the structural property that separates it from legitimate content, not for a list of the bad values. The obvious implementation here was a blocklist of structured-write field names (</context>, </impact>, …), which is both incomplete (every new tool argument adds a name) and destructive (a lesson discussing XML loses content). The structural property is BALANCE: leaked framing is an unbalanced closing tag, real markup in prose is balanced. That one predicate is complete, needs no maintenance as the tool surface grows, and provably cannot touch well-formed content. Pair it with the vocabulary that is unambiguous on its own (parameter/invoke/function_calls) and with a code-span exemption, so an entry that QUOTES the bad shape — documentation, tests, changelog — survives; the read side already established that exemption, and a write-side guard without it will eventually corrupt the very docs that describe it. *(evidence: Phase 2: src/clauderizer/mutations.py _strip_toolcall_markup (unbalanced-closer predicate + _code_segments); tests/test_toolcall_write_guard.py::test_balanced_markup_is_left_alone, ::test_markup_inside_a_code_span_is_untouched, ::test_an_opener_and_its_closer_may_straddle_a_code_span)*

**6.** A function that returns the same value for "nothing here" and "this failed" has no failure mode — it has a silence. model.from_file returned None both for ordinary prose and for an entity doc it could not read, so a defective node did not error, it stopped existing, and every downstream consumer reasoned about a DAG that was quietly short a node. The repair is a three-way return (value / absent / DROP-with-reason), and the two disciplines that make it worth anything: (1) the failure record must carry the PATH and a machine-readable reason, because a count is not actionable — "1 entity doc not indexed" sends nobody anywhere; (2) publish an ACCOUNTING IDENTITY (indexed + dropped + shadowed == files seen) so a caller can prove nothing fell out between the scan and the report, rather than trusting that it didn't. And classify conservatively: only a file that clearly INTENDED to be the thing counts as a failure, or the count acquires standing false positives and stops being read. The payoff is never in the loader — it is at the consumer that was answering confidently from the gap (cz_cascade's "ok:true, 0 dependents" for a node that was never in the graph). *(evidence: Phase 3: model.Drop + _classify_drop; graph/index.py Collision + Graph.integrity(); ops.cz_cascade unknown-entity guard. RED at efdf210: a BOM'd subsys.probe gave from_file->None and cz_cascade->ok=True with 0 dependents)*

### Category: Testing

**2.** "Demonstrated RED on the pre-fix tree" is satisfied by an ImportError only in the letter. A new module means the new test file cannot even collect on the old tree, which proves the module is absent — not that the BEHAVIOR was absent, which is the thing the criterion is actually about. Build the red demonstration as a standalone probe that uses ONLY APIs present on both trees, drives the same fixture the tests build, and prints the old engine's actual answer. Here that turned a collection error into the real finding: the pre-1.14.1 engine reported `preflight PASS` and a digest with no lag line, against a repo whose tracker read NOT STARTED with a src/ commit sitting past it — the 1.14.0 failure reproduced on demand, in the old engine's own words. Run the same probe post-fix and the two outputs are the before/after the phase summary needs. Prefer `git clone --local --no-checkout` + `checkout --detach` over `git worktree add` for the isolated old tree: a clone writes nothing into the real repo's .git, so the isolation is structural rather than promised (L-29). *(evidence: Phase 0: scratchpad red_probe.py run under PYTHONPATH=<clone>/src at efdf210 vs the working tree; recorded in output RED_BEFORE_GREEN_HARNESS)*

**4.** Run the detector against the REAL machine, not only the fixture — the fixture proves the mechanism, the machine tells you the size of the problem. The two nested fixture repos proved ownership works; pointing the same scan at the actual home directory found TEN nested installs and surfaced the root cause the gameplan had not stated: /home/ccusce/.claude/settings.json IS the per-user settings file, so clauderizing $HOME makes that install's hook global to every session on the machine. Corollary for anything wired through a package runner: a live green on the mechanism is NOT a live green on the deployment. The outer install's hook runs `uvx --from clauderizer`, i.e. the PUBLISHED engine, so the repair does not reach the very install that exhibits the pathology until the release ships and the runner's cache refreshes — record that gap as an explicit output and re-run the live check after publish, rather than letting "verified live" read as done. *(evidence: Phase 1 outputs LIVE_NESTED_SCAN (10 installs, 0.24s) and DEPLOYMENT_GAP; /home/ccusce/.clauderizer/hook.sh line `uvx -q --from clauderizer clauderizer-hook`)*

## Project Lessons (most relevant to this phase)

_(5 of 25 shown in full — ranked by keyword + entity-id overlap with this phase, no ML. Every other active lesson is indexed below; nothing is dropped (D-068). The full text is canonical in `docs/LESSONS.md` — fetch any entry with `cz_get`.)_

**L-51.** Before any irreversible release step (tag → GitHub Release → PyPI publish), gate on three sweeps. (1) A version number is a claim across four registries that never sync — source (pyproject/__version__), remote git tags, GitHub Releases, the PyPI index — plus the ordering invariant that origin/main must hold the staged commit BEFORE any tag/Release (a GitHub-UI release tags the REMOTE head, so locally-authored guards are unpushed by construction); sweep all four with fresh eyes (uvx by-name answers from uv's cache and can hide a failed attempt — v0.7.0/v0.8.0 were double-claimed by exactly this). (2) Run the suite on EVERY host leg the CI matrix covers — a green on one OS is a guess about the others and the publish cannot be undone; a path/separator assertion is itself a platform claim (assert the wrapper FILE hook.sh|hook.cmd, not the slash) — 0.14.0 shipped with 3 Windows cells red on such an assertion. (3) release-check's clean_tree counts UNTRACKED files (foreign tool artifacts, regenerable caches) as a dirty tree and blocks the ritual even though the published artifact builds from origin/main + the tag; the surgical honest fix is .git/info/exclude (local-only, non-committed, deletes nothing) for files that aren't this repo's content — verify `git status --untracked-files=no` is empty first to prove no real source change is uncommitted. (Consolidates L-31 [itself L-08+L-20], L-42.) *(from 2026-07-16-hotpatch-lesson-redistill-and-proposal-triage)*
**L-25.** A health check / guard must verify CAPABILITY, not presence - a green check on a non-launchable setup is worse than none. Prove every probe at the granularity it reports, in BOTH directions (it must fire on the failure it exists for and stay green on health, per matrix cell / per argument), and prefer in-band identity (the output must claim who and what version ran) over exit codes: locally-sound guards compose into false green (a wrapper that always exits 0 defeats a spawn probe that reads exit 0 as launchable). The probe must traverse the consumer's exact execution leg, shell quirks included. (Consolidates L-02, L-09, L-10.) *(from 2026-06-20-empirical-memory-gains)*
**L-21.** Reference docs drift together on a taxonomy change. When hook events or the tool surface change, sweep every non-single-sourced present-tense doc at once: README's MCP-surface tool list, TRUST.md (what init writes / what executes), UPGRADING.md's uninstall script, and SECURITY.md's scope line. The single-sourced CLAUDE.md/AGENTS.md stanza (L-16) is safe; those four are not. Append-only history (CHANGELOG, gameplan handoffs and cascade reports) records the OLD counts on purpose — never 'correct' it to the new number. *(evidence: 0.14.0 docs audit (2026-06-20): README MCP surface listed 24 tools vs tools_list.py's 31; preflight 7-vs-8 checks; TRUST/UPGRADING/SECURITY still described pre-0.14 SessionStart-only wiring (no UserPromptSubmit / AGENTS.md / kimi-setup).)* *(from 2026-06-19-kimi-lifecycle-integration)*
**L-62.** When a doc enumerates a code-owned surface (a tool list, a version header, a shipped-artifact claim), make the seam EXECUTABLE — a test that diffs the doc against the source of truth — rather than relying on sweep discipline. Extends L-21/L-55: the prose sweep found README's MCP surface silently 14 tools behind (the whole 1.12.0 listing contract missing); the pin test (README backticked names vs TOOL_NAMES + the count line) and the procedure-version parity test kill the drift CLASS, not the instance. First dream-sourced promotion: the gap was captured as a dream note mid-build, distilled by cz_dream, staged, triaged, and accepted (D-058/D-059 loop, gameplan 2026-07-23-dreaming-loop). *(from 2026-07-23-dreaming-loop)*
**L-23.** The author's environment never exercises the real thing - a monkeypatched-platform test is not the platform, and an editable venv is not uvx --from PyPI. Execute the real surface: preview a foreign CI cell in a native venv on the target OS (one local cycle caught every win32 defect nine months of mocked-platform tests missed), and walk the published install/distribution path from a fresh environment (the README first command was broken in four places while every test passed). Then pin each as a CI job that runs the doc-exact text, with assertions that self-arm when the fix they guard is unreleased. (Consolidates L-12, L-13.) *(from 2026-06-20-empirical-memory-gains)*

### All Active Project Lessons (25)

_(id + first line. The ranker above chooses what to render in full; it does not choose what you are allowed to know — a pointer, never an authority (D-013).)_

- **L-11** — Declare phase dependencies by technical need, not narrative order — and expect a restart-gated exit criterion to split its phase across sessions by…
- **L-14** — A dependency's footprint relative to the data it serves is a first-class go/no-go axis — it can disqualify a tool before functional quality is ever…
- **L-21** — Reference docs drift together on a taxonomy change.
- **L-23** — The author's environment never exercises the real thing - a monkeypatched-platform test is not the platform, and an editable venv is not uvx --from…
- **L-24** — A 'degrades gracefully' claim is only as strong as the input diversity it was tested against (non-dict valid JSON, BOM/CRLF, unicode, empty), AND t…
- **L-26** — Re-distill curated, append-only memory behind a COVERAGE GATE, never by taste: auto-derive a query from each lesson's OWN distinctive tokens (no ha…
- **L-29** — Isolate destructive/irreversible operations from the real repo BEFORE running them, and prove the isolation — never assume it.
- **L-33** — A subagent's output needs two guards.
- **L-41** — An identity default (driven kind = identity lexicon, empty preflight list) lets a large generalization land with ZERO behavior change for the exist…
- **L-57** — Curating docs/LESSONS.md DOWN is consolidate + obsolete (add a synthesis via cz_add_lesson→cz_promote_lesson, then cz_obsolete_lesson each source p…
- **L-58** — A host whose MCP config is a single per-user file (shared by every repo the host opens) must get a REPO-AGNOSTIC server command — one that discover…
- **L-59** — A host that REGENERATES its own MCP config on context-switch and merges from no persistent source cannot bootstrap its own re-heal from within — on…
- **L-60** — A registry populated by IMPORT SIDE-EFFECTS (module-top `register(Impl())`) is EMPTY until something imports the implementation module — and an in-…
- **L-61** — Durable per-host OVERRIDE state must NOT live only in a config the host regenerates.
- **L-62** — When a doc enumerates a code-owned surface (a tool list, a version header, a shipped-artifact claim), make the seam EXECUTABLE — a test that diffs…
- **L-25** — A health check / guard must verify CAPABILITY, not presence - a green check on a non-launchable setup is worse than none.
- **L-07** — Design failure reporting backward from the channel the consumer actually reads, then place the reliability wrapper on the deepest layer whose failu…
- **L-52** — Round-trip idempotency (apply-twice == apply-once) through the engine's own parser is the load-bearing test for every mutation — every file the eng…
- **L-53** — A self-improving memory system rests on two pillars.
- **L-56** — The memory tool surface has two load-bearing contracts.
- **L-55** — Independent surfaces that must stay in agreement drift SILENTLY unless a test pins the seam — add the test (or an independent post-implementation r…
- **L-50** — Treat every claim, borrowed idea, audit finding, or "realize-the-win" as a FALSIFIABLE hypothesis with a pre-named machine-checkable metric, and ME…
- **L-51** — Before any irreversible release step (tag → GitHub Release → PyPI publish), gate on three sweeps.
- **L-48** — Exclusive --host is the wrong default for multi-AI repos: wire all project-level hosts by default (enabled=["*"]), keep --host as a scope filter, d…
- **L-54** — Don't assume a non-Claude host/model or a "stale"-looking path behaves as you expect — verify the reality first.

## Ending Protocol

1. `cz_transition_phase` the finished phase to complete.
2. `cz_add_output` each concrete produced value; `cz_add_phase_summary` the recap; `cz_add_correction` / `cz_add_lesson` as earned.
3. `cz_transition_status` on touched entities (fires cascade); `cz_resolve_cascade` the verdicts.
4. `cz_write_handoff` for the next phase.
5. Run exit verification; report the test count.
<!-- clauderizer:handoff:end -->

## Phase Notes (agent-owned)

_(Enrich here: what exists / what does not yet, key constraints, captured
source-of-truth values for this phase. Everything outside the marker block
above survives `cz_write_handoff` regeneration.)_
