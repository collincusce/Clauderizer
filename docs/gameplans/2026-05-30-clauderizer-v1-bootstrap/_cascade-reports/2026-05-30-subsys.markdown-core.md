# Cascade Report: subsys.markdown-core status planned -> completed

> Generated: 2026-05-30T21:48:13Z

## Trigger

`subsys.markdown-core` — status planned -> completed

## Affected entities

### Direct dependents

- **subsys.graph** (status: completed) — checked: no change needed; consumes the stable frontmatter/parse API only.
- **subsys.mutations** (status: completed) — checked: no change needed; writes exclusively through `markdown/writer.py`, whose surface is unchanged.
- **subsys.rituals** (status: completed) — checked: no change needed; reads sections via the public `markdown/sections.py` helpers.
- **subsys.scaffold** (status: completed) — checked: no change needed; uses `create_if_absent` + marker-block writes only.

### Transitive dependents

- **subsys.mcp-server** — flagged via subsys.graph/rituals/mutations; checked: no change needed (it composes those subsystems, no direct markdown-core coupling).
- **feat.init-cli** — flagged via subsys.scaffold; checked: no change needed.

## Updates applied

None required. `subsys.markdown-core` reached `completed` with its public API
(frontmatter parse/serialize, section/marker edits, the single `writer.py`
mutation path) unchanged from what dependents already pinned at `^0.1.0`.

## Updates deferred

None.
