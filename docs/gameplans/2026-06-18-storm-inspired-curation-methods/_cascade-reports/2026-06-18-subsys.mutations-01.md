# Cascade Report: subsys.mutations version 0.5.0 → 0.6.0: optional evidence/provenance added to add_lesson + add_decision (additive, backward-compatible)

> Generated: 2026-06-18T06:58:22Z

## Trigger

`subsys.mutations` — version 0.5.0 → 0.6.0: optional evidence/provenance added to add_lesson + add_decision (additive, backward-compatible)

## Affected entities

### Direct dependents

- **subsys.mcp-server** (status: active) — checked: no change needed — additive MINOR bump; 0.6.0 satisfies the ^0.1.0 pin and the new evidence param is optional (omitted = byte-identical output), so the exposed tool surface and behavior are unaffected

### Transitive dependents

- _(none)_

## Updates applied

None — additive, backward-compatible MINOR bump (optional evidence param + inline render); no dependent required any change. cz_add_lesson/cz_add_decision MCP schemas gained the optional param (auto-derived); existing callers unaffected.

## Updates deferred

_(anything flagged but not yet acted on, with reason + follow-up)_
