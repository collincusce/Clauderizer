# Hardening

**Append-only** persistent risk tracker. NEVER delete entries — mark a risk
resolved with a date instead. This is a permanent audit trail. Numbered `H-NN`.

## Risks

### H-01 — Checked-in launch wiring was non-launchable on the working machine; cold start silently lost the digest and every cz_* tool

- **Severity**: high
- **Status**: resolved (2026-06-09)
- **Affected**: .mcp.json; .claude/settings.json; scaffold/init.py command resolution; CLAUDE.md stanza (documents cz_status but no CLI fallback)
- **Invariant violated**: L-02: health checks must verify capability, not presence - the committed wiring itself was never capability-checked on the host that runs sessions
- **Preconditions**: Session host without uvx on PATH (here: both Windows host and the WSL distro)
- **Impact**: On this machine the SessionStart hook and MCP server both invoked uvx, which is installed neither in WSL nor on Windows. A cold session got no [Clauderizer] digest and no cz_* tools - the entire blessed-write surface was unreachable - and nothing in-session surfaced the failure: a hook that cannot spawn prints nothing, and a missing MCP server just looks like an un-clauderized repo.
- **Root cause**: Wiring files were committed with the uvx fallback command rather than the venv console scripts; doctor catches the breakage but only when run by hand - the cold-start path has no breadcrumb when the hook dies
- **Reproduction**: Start a Claude Code session on a host without uvx: no digest, no cz_* tools. `clauderize doctor` (repo source) -> 'MCP server command runnable' and 'SessionStart hook command runnable' both FAIL
- **Recommended fix**: Local wiring repaired this session: editable venv reinstalled and `clauderize init` re-run from .venv, doctor now all-green. Engine-side (open): the CLAUDE.md stanza should name the CLI fallback (`clauderize status`) so a session without MCP can self-orient; doctor could also compare the executed engine version against the repo source version when the repo IS the engine (dogfood skew).
- **Regression tests**: None yet - candidate: init warns loudly when the command it writes does not resolve on the current host
- **Resolution**: Residue closed by agent-autonomy Phase 3 (D4): init now writes a breadcrumb wrapper (.clauderizer/hook.sh, hook.cmd for native-win32) and registers IT as the SessionStart command; the wrapper invokes the engine hook, forwards args (probe-transparent), and converts any engine failure into a STDOUT breadcrumb '[Clauderizer] engine unreachable: exit N from <engine> - run clauderize doctor' + the captured error, always exit 0. Live demo (2026-06-10, scratch repo /tmp/breadcrumb_demo wired windows-wsl:ubuntu, engine binary renamed): pre-D4 direct wiring -> exit 127, stdout EMPTY (error stderr-only - the silent cold start); registered wrapper command run verbatim from PowerShell -> exit 0, breadcrumb + captured 'not found' on stdout; binary restored, clauderize --version green. Regression: tests/test_hook_wrapper.py (real /bin/sh execution: dead engine -> verbatim breadcrumb on stdout/exit 0, healthy passthrough, arg forwarding; init registration matrix + upgrade dedup + regeneration; doctor presence/freshness/nudge checks - 16 checks on wrapper repos). REMAINING SILENT BOUNDARY (documented, accepted): the wrapper itself failing to spawn - wsl.exe/the distro dead (then the \\wsl.localhost repo is unreachable and no session starts here anyway), /bin/sh absent, or the wrapper file deleted (doctor 'hook wrapper present' catches that on the next run). One layer below the wrapper there is no engine-side code left to speak.
### H-02 — Blessed phase-table writes emit invalid markdown tables, and the engine's own parser hides it

- **Severity**: medium
- **Status**: resolved (2026-06-09)
- **Affected**: mutations.add_phase (row appended via writer.append_to_section as a paragraph); templates/gameplan/CHAT-HANDOFF-INDEX.md (legend inside the table's section, directly after row 0); rituals/_tables.parse_phase_table (tolerates non-table layout)
- **Invariant violated**: Extends L-01/L-04: round-tripping through the engine's own parser is necessary but not sufficient - written artifacts must be valid for external renderers too
- **Preconditions**: Any gameplan with more than one phase
- **Impact**: Every gameplan's Phase Status Table renders broken for humans: rows appended after phase 0 are separated by blank lines - and in CHAT-HANDOFF-INDEX by the legend paragraph - so renderers show a one-row table plus orphaned row fragments. parse_phase_table scans any pipe-prefixed line in the section, so cz_status reads the corrupt layout fine and round-trip tests pass. Evidence: all four tracker docs of the two closed 2026-06-09 gameplans.
- **Root cause**: Phase rows are appended as new paragraphs at the end of the section instead of being inserted into the existing table block
- **Reproduction**: cz_add_phase twice on a fresh gameplan, then render CHAT-HANDOFF-INDEX.md: blank line and legend split the table
- **Recommended fix**: Table-aware row insertion in the writer (insert after the last contiguous row of the section's first table block); move the legend out of the table section in the template; add a render-validity regression (no blank lines or prose between a table's rows). One-time repair of the four existing broken trackers via the fixed write path.
- **Regression tests**: None yet - candidate listed in recommendation
- **Resolution**: 0.6.0 Phase 0: rows write through markdown/tables.py (contiguous block rebuild); all six fractured trackers healed in place by blessed touches; render-validity regression tests added.
### H-03 — Memory gauge silently drops the handoff-size estimate exactly when a gameplan completes

- **Severity**: low
- **Status**: resolved (2026-06-09)
- **Affected**: rituals/status_bundle.compute (token estimate gated on a target phase); render_digest (omits the figure with no explanation)
- **Preconditions**: Active gameplan with all phases complete
- **Impact**: CHANGELOG 0.5.0 and the gameplan exit criteria promise 'Memory: N active lessons, M project (~K tok handoff)'. The token estimate is computed only when a current or next phase exists, so a completed gameplan's digest - the close-out moment when memory weight matters most - shows no size and nothing explains the absence. Observed on this repo's own digest today.
- **Root cause**: The estimate sizes the next phase handoff; a finished gameplan has no target phase, and the gauge is deliberately best-effort-silent
- **Reproduction**: `clauderize status` on this repo (all phases of 2026-06-09-context-economics complete): memory line reads 'Memory: 0 active lessons, 4 project.' with no token figure
- **Recommended fix**: Size a synthetic close-out bundle (project lessons + decisions still ride into the next gameplan), or render an explicit 'handoff size n/a - gameplan complete' so the absence is explained rather than silent
- **Regression tests**: None yet
- **Resolution**: 0.6.0 Phase 2: completed-gameplan digest renders '(handoff n/a: gameplan complete)' instead of silently dropping the size.

### H-04 — Repaired wiring was host-scoped: Windows-host session cold-started with no digest and no cz_* tools while doctor reported 13/13 green

- **Severity**: high
- **Status**: resolved (2026-06-09)
- **Affected**: .mcp.json; .claude/settings.json; scaffold init --run-cmd composition; doctor launchability checks (host-blind)
- **Invariant violated**: L-02 one layer up again: capability was verified, but in the wrong environment - a runnability check is only as good as the host it spawns from
- **Preconditions**: Session host (Windows Claude Code over \\wsl.localhost) differs from engine host (WSL venv); wiring written as WSL-native absolute paths
- **Impact**: On the Windows host (this machine's primary session entry point, repo opened via \\wsl.localhost UNC), the SessionStart hook and MCP server commands were WSL-native venv paths Windows cannot exec: no [Clauderizer] digest, no cz_* tools, no in-session breadcrumb - H-01's exact impact, reintroduced one layer up by the H-01 fix itself. Meanwhile clauderize doctor, running inside WSL, certified the same wiring runnable (13/13 green), so the health check actively misled for the host that consumes sessions.
- **Root cause**: init writes one wiring for one host and has no host-of-record concept. A composition bug compounds it: init --run-cmd 'wsl.exe -d ubuntu <venv>/bin/clauderize' appends the console-script name as a CLI argument, producing 'clauderize clauderizer-mcp' / 'clauderize clauderizer-hook' - invalid subcommands argparse rejects (exit 2) - so init cannot express split-host wiring at all
- **Reproduction**: From Windows: open a session on the UNC repo with WSL-native wiring -> no digest, no tools, doctor (in WSL) all green. Composition bug: clauderize init --run-cmd 'wsl.exe -d ubuntu /home/ccusce/Clauderizer/.venv/bin/clauderize', then echo '{}' | wsl.exe -d ubuntu /home/ccusce/Clauderizer/.venv/bin/clauderize clauderizer-hook -> exit 2 invalid choice
- **Recommended fix**: init: accept a session-host-of-record (e.g. --session-host windows-wsl) and compose wsl.exe-shimmed console-script wiring (command/args split for .mcp.json, single command string for the hook); doctor: spawn the wiring's exact commands from the recorded session host, or at minimum flag a host mismatch instead of certifying from the wrong side; until then the two wiring files are hand-maintained
- **Regression tests**: None yet - candidate: init round-trip test asserting that for any --run-cmd form, the composed wiring argv is accepted by the binary it names (spawn with --help)
- **Resolution**: Closed by agent-autonomy Phase 2 (D3): config records a session host of record ([host] session_host, native | windows-wsl:<distro>); init composes the wsl.exe shim from it (command/args split for .mcp.json, one command string for the hook) and spawn-tests every composed command with --version before writing, refusing on failure (WiringRefused; src/clauderizer/hosts.py); doctor verifies launchability FOR the recorded host via a real wsl.exe interop round-trip or honestly reports 'unverifiable from this host' (exit 3, never a false green). Evidence: clauderize init on this repo regenerated the hand-maintained wiring byte-identically (.mcp.json and settings.json 'kept', config gained the session_host line) and doctor through the PowerShell shim certified 14/14 with 'verified end-to-end via wsl.exe round-trip (clauderizer 0.6.0)'. Regression: tests/test_hosts.py (composition matrix, the exact 'clauderize clauderizer-mcp' exit-2 shape refusing init with nothing written, doctor 3-state messaging). Residual boundary (documented): a mid-session wiring repair still needs a session restart to attach MCP tools - harness enumerates servers at session start.

### H-05 — No write lock on tracked docs: concurrent sessions on one repo can silently lose updates or duplicate IDs

- **Severity**: medium
- **Status**: resolved (2026-06-09)
- **Affected**: mutations.py (all 18 public write functions); .clauderizer index cache; any multi-window or multi-agent workflow on one repo
- **Invariant violated**: Append-only trackers assume appends are serialized; numbering assumes allocation+write is atomic
- **Preconditions**: Two writer processes (MCP servers, future CLI ops, or shims) mutating the same repo concurrently; sub-second collision window per write
- **Impact**: Every mutation is read-modify-write on markdown with no inter-process lock (verified: no flock/lockfile/mutex anywhere in src/clauderizer; the only 'lock' is profile.lock.toml, a pin file). Two sessions - or one session plus a subagent - mutating the same repo in the same window can last-writer-wins erase each other's appends, and two concurrent ID allocations (H-NN, L-NN, D-k, A-NNN) can both compute the same next number. Cross-project concurrency is unaffected: state is per-repo by construction (server per session, repo = spawn cwd).
- **Root cause**: The engine assumes one interactive session per repo, but stdio MCP spawns one server process per session - the assumption breaks the moment a second window or subagent opens the same project
- **Reproduction**: Open two sessions on one repo and fire cz_add_lesson in both within the same second; inspect numbering and the lessons section for a lost append or duplicate number
- **Recommended fix**: Advisory lock in the single mutation path (D-007 makes it one choke point): e.g. .clauderizer/write.lock created O_EXCL with a stale-lock timeout, held per mutation. Covers MCP, CLI ops, and shims uniformly; read tools stay lock-free (L-03). Until then: one writer session per repo at a time; reads are always safe; git history is the recovery backstop. Candidate task for the agent-autonomy gameplan Phase 0 alongside clauderize ops
- **Regression tests**: None yet - candidate: N concurrent add_lesson processes against one repo yield N distinct sequential numbers and N surviving entries
- **Resolution**: Closed by agent-autonomy Phase 0: advisory write lock at the mutation choke point (src/clauderizer/locking.py; all 18 public mutations.* functions serialize on .clauderizer/write.lock, reentrant, stale takeover ~30s). Regression evidence: tests/test_locking.py::test_concurrent_writer_processes_lose_nothing (8 concurrent writer processes -> 8 distinct sequential ids, 8 surviving appends) and test_crashed_holder_blocks_at_most_stale_timeout. This resolution was itself written through the locked path.

### H-06 — Stale pinned engine wiring certified green — launchability probes cannot distinguish engine identity

- **Severity**: medium
- **Status**: resolved (2026-06-09)
- **Affected**: hosts.verify_wiring (round-trip verdict); doctor launch checks; any uvx/pipx-pinned or cached wiring; the D4 hook wrapper's probe surface
- **Invariant violated**: L-02, third recurrence one layer up: launchability was verified but identity was not — a green check that speaks for a different engine than sessions get is worse than no check
- **Preconditions**: Wiring resolves an older engine that answers probes exit-0: pre-0.6.0 entry points exit 0 on --version without printing it (the FastMCP EOF accident, gameplan lesson #4) when the [mcp] extra is present; or any engine failure behind the always-exit-0 hook wrapper
- **Impact**: A repo wired to a pinned or cached old engine passes every exit-code probe: init's spawn test legitimately writes the wiring and the pre-0.7.0 doctor certifies it 'verified end-to-end via wsl.exe round-trip' — so sessions silently run stale code while health checks read green. Worse, composition with the D4 breadcrumb wrapper (always exit 0 by design, converting engine death into a stdout breadcrumb) turned even a DEAD engine into a green hook verdict.
- **Root cause**: Spawn probes judge exit codes only. Pre-0.6.0 entry points exit 0 without identifying themselves, and the D4 wrapper deliberately exits 0 on engine failure (its job is breadcrumbing the session context) — two locally-sound contracts composing into a false green at the doctor layer
- **Reproduction**: Scratch repo: clauderize init --run-cmd '<abs-uvx> --from clauderizer[mcp]==0.5.0' --session-host windows-wsl:ubuntu — init writes it WITH the spawn test on (both 0.5.0 entry points exit 0, observed live 2026-06-09). Pre-0.7.0 doctor: both launch checks green. Note the bare pin clauderizer==0.5.0 (no [mcp]) is instead refused at init: clauderizer-mcp exits 1 on the missing mcp import — the launch guard catches launch-dead wiring; only launch-alive-but-stale needed a new guard
- **Recommended fix**: verify_wiring requires the successful round-trip to identify its engine: the probed --version output must claim 'clauderizer <running __version__>' (hosts.served_version); fail loudly on a missing claim (pre-0.6.0 pin, wrapper breadcrumb) or a version skew (stale pin/cache), naming both versions and the remedies
- **Regression tests**: tests/test_hosts.py: round-trip certifies only the running version (the old fixture certifying 'clauderizer 9.9.9' flipped into the skew test); fails on skew; fails without identity (the exit-0/no-output accident); fails on the wrapper breadcrumb shape
- **Resolution**: Closed by agent-autonomy Phase 4 (0.7.0): hosts.served_version + identity branches in verify_wiring — a successful round-trip must claim 'clauderizer <running __version__>' or the verdict fails loudly (missing claim: 'did not identify its engine'; mismatch: 'wiring serves clauderizer X but this doctor is Y'). Live demo 2026-06-09 (D5, output captured in the Phase 4 close-out): scratch repo /tmp/stale_uvx_demo wired '<abs-uvx> --from clauderizer[mcp]==0.5.0' via clauderize init WITH the spawn test on — init legitimately wrote it (both 0.5.0 entry points exit 0; the lesson-#4 EOF accident observed live, and 0.5.0's hook even answered with its 0.5.0-era digest: a working-but-wrong engine). The CURRENT doctor probing through that exact wiring: MCP launch check FAIL ('did not identify its engine, expected clauderizer 0.7.0'), hook launch check FAIL (0.5.0 digest output carries no version claim), exit 2. Healthy wiring control: this repo's doctor 16/16 green, both checks certifying '(clauderizer 0.7.0)', exit 0. By-name half: uvx --from clauderizer resolves 0.6.0 (PyPI latest). Boundary recorded: the bare pin clauderizer==0.5.0 (no [mcp] extra) is refused at init time instead (clauderizer-mcp exit 1 on missing import) — launch guard catches launch-dead, identity guard catches launch-alive-but-stale. Regression: tests/test_hosts.py identity tests (incl. the flipped 9.9.9 fixture and the wrapper-breadcrumb false-green).

### H-07 — Release pipeline has no tag-vs-source version guard: a v0.7.0 Release at a 0.6.0-source commit failed publish as a silent PyPI duplicate

- **Severity**: medium
- **Status**: resolved (2026-06-10)
- **Affected**: .github/workflows/publish.yml (no version-consistency gate); the release procedure (no remote-namespace sweep before claiming a version); doctor (engine-identity checks cover source/dist-info/wiring, nothing covers tag-vs-source)
- **Invariant violated**: H-06's pattern one registry up: exit/launch state was checked where identity was required — a Release is an identity claim ('this tag IS version N') that nothing verified
- **Preconditions**: A Release/tag cut from any commit whose pyproject version does not equal the tag; or any release staged without enumerating remote tags + GitHub Releases + PyPI
- **Impact**: A GitHub Release v0.7.0 (published 2026-06-09T22:24Z from the Phase-0 commit 6f8afc1, whose pyproject still declared 0.6.0) triggered publish.yml, which built 0.6.0 artifacts and failed as a PyPI duplicate (run 2026-06-09T22:30Z, conclusion: failure). Result: the repo's Releases page claims v0.7.0 exists while pip/uvx can never install it — and nothing in-repo surfaces the skew. Compounding: the version number is claimed across four registries that do not sync (source version, remote tags, GitHub Releases, PyPI index); the Phase-4 release staging swept three and missed the remote tag + Release entirely, double-claiming 0.7.0.
- **Root cause**: publish.yml builds whatever the tagged commit declares and lets twine discover the collision; tags are created by hand with no check that the tagged tree's version matches the tag name
- **Reproduction**: Cut a GitHub Release vX.Y.Z from a commit whose pyproject declares an older, already-published version: the workflow builds the old version and the upload fails as a duplicate; the Release object remains published either way
- **Recommended fix**: (1) publish.yml gains a pre-build gate: extracted pyproject version must equal the release tag (strip leading v) or the job fails with a message naming both. (2) Release procedure: before claiming version N, sweep all four registries — git tag -l, git ls-remote --tags origin, GitHub Releases API, PyPI releases (fresh, not via uv cache). (3) Resolve the current double-claim of 0.7.0 (retarget/recreate the Release at the real release commit, or renumber the staged release).
- **Regression tests**: The publish.yml gate is the regression guard (CI-level; it fails the exact 6f8afc1 shape). Procedure-level: the sweep is now lesson #9 in the agent-autonomy handoff roll-up
- **Resolution**: Closed 2026-06-10. (1) publish.yml now refuses tag/source skew before building: the gate compares GITHUB_REF_NAME (v-stripped) against the tagged tree's pyproject version and fails naming both — the exact 6f8afc1 shape (tag v0.7.0, source 0.6.0) is rejected up front instead of dying as an opaque twine duplicate. (2) The double-claim resolved by retiring 0.7.0: staged release renumbered to 0.8.0 (user's call: keep the existing Release rather than retag), CHANGELOG carries a '0.7.0 — version retired, never published' tombstone explaining the pip-visible hole. (3) The four-registry sweep (local tags, remote tags, GitHub Releases, PyPI direct — never via uv's cache) recorded as gameplan lesson #9. ACCEPTED RESIDUAL (user choice): the v0.7.0 Release/tag remain on GitHub pointing at the Phase-0 commit; the Release page implies a version that will never be installable. The gate prevents recurrence; it cannot heal the existing object.

### H-08 — Windows harness executes SessionStart hooks via Git Bash; MSYS2 path conversion mangles the wsl.exe shim argv — digest never injected

- **Severity**: high
- **Status**: resolved (2026-06-10)
- **Affected**: SessionStart command emitted into .claude/settings.json by hosts.py wiring composition; clauderize doctor 'SessionStart hook launchable — verified end-to-end via wsl.exe round-trip' probe (does not traverse the harness's Git-Bash executor); .clauderizer/hook.sh breadcrumb coverage (failure sits below the deepest observable layer, per lesson #6).
- **Invariant violated**: H-01: a session whose engine cannot launch must still learn why — violated silently (wrapper never executes, so the breadcrumb cannot fire). Also L-02 (health checks must verify capability through the consumer's exact leg) and H-06 (in-band identity beats exit codes).
- **Preconditions**: Claude Code harness on Windows executing hook commands through Git Bash (bash -c) with Git for Windows installed; project wiring's hook command carries bare POSIX-path argv elements (/bin/sh, /home/...) intended for the WSL distro.
- **Impact**: On the windows-wsl reference host, no harness session receives the [Clauderizer] digest: the registered SessionStart command dies with exit 127 BELOW the D4 wrapper, so neither the digest nor the H-01 breadcrumb reaches session context — silent absence. Both wrapper-era cold starts confirmed affected (Phase-4 session 228fb4d0 and restart-validation session 6b9a162f, identical failure attachments). Blocks v0.8.0 restart validation; doctor meanwhile reports 16/16 green — false confidence in the hook leg.
- **Root cause**: The harness runs hook commands via Git Bash, whose MSYS2 automatic POSIX-to-Windows argument conversion rewrites /bin/sh to C:/Program Files/Git/usr/bin/sh before wsl.exe spawns. wsl.exe forwards the mangled Windows path into Ubuntu, whose default-shell wrapper (/bin/bash -c) cannot exec it: '/bin/bash: line 1: C:/Program Files/Git/usr/bin/sh: No such file or directory', exit 127. The command string is correct; the executing shell's argv rewriting breaks it. Trust ruled out (hasTrustDialogAccepted=true; hooks DID run), timeout ruled out (harness recorded durationMs=383; manual run 119ms), stdin ruled out (digest emits with harness-style JSON piped).
- **Reproduction**: Verified outside the harness from PowerShell: & 'C:\Program Files\Git\bin\bash.exe' -c "wsl.exe -d ubuntu /bin/sh /home/ccusce/Clauderizer/.clauderizer/hook.sh" → exit 127, stderr byte-identical to the hook_non_blocking_error attachments at line 5 of transcripts 228fb4d0-6e96-4b6c-92e1-caa1348451b1.jsonl and 6b9a162f-4bf1-4598-b20f-97eb5966c920.jsonl. Same command from plain PowerShell: digest + exit 0 in ~119ms (also with stdin JSON) — defect exists only in the Git-Bash leg.
- **Recommended fix**: (1) hosts.py must emit a hook command robust under the harness's executing shell. Verified working through Git Bash: wsl.exe -d ubuntu sh -c 'exec /home/ccusce/Clauderizer/.clauderizer/hook.sh' (no bare-POSIX-path argv for MSYS to rewrite) and MSYS_NO_PATHCONV=1 wsl.exe -d ubuntu /bin/sh ... (GfW-specific). Both MUST be re-validated under cmd.exe/PowerShell executors before adoption (env-prefix is bash-only; PowerShell strips single quotes). Candidate universal shape to test: leading double-slash paths (//bin/sh — MSYS skips conversion, Linux collapses //). Add a wiring regression test matrix across executing shells. (2) doctor's hook-launchability probe must traverse Git Bash when present (the consumer's actual leg) or weaken its claim wording; in-band restart validation remains the only true end-to-end proof.
- **Regression tests**: None yet — owed with the fix: shell-matrix wiring composition test (Git Bash / cmd / PowerShell executors) and a doctor probe-through-bash test.
- **Resolution**: 2026-06-10, Phase 1 of harness-truth-and-release-ritual. Fix per D2: hosts.py emits shape C — wsl.exe -d ubuntu //bin/sh //home/ccusce/Clauderizer/.clauderizer/hook.sh (//-led paths: MSYS2 skips conversion per its UNC convention; Linux collapses // to /) — plus the H-09 self-anchoring wrapper. Executor matrix (scripts/wiring_matrix.ps1, hostile cwd C:\) green for C under all three executors; CUR control fails only under Git Bash, proving the matrix detects this defect. LOAD-BEARING RESTART EVIDENCE (the gate this finding was held open for): cold start 2026-06-10, transcript e4573a6d-1f63-4ab9-b563-5477011b4255.jsonl, hook_success attachment for SessionStart:startup at ts=2026-06-10T16:53:26.319Z — command=wsl.exe -d ubuntu //bin/sh //home/ccusce/Clauderizer/.clauderizer/hook.sh (registered shape C verbatim), exitCode=0, durationMs=388, stderr empty, stdout = the full [Clauderizer] digest ("Gameplan 2026-06-10-harness-truth-and-release-ritual: phase 1/5 IN PROGRESS — \"hosts.py emits the immune shape; restart-validate H-08\"…" + Baseline/Memory/Tools lines), injected into session context and acted on by that session (it executed this close-out). Same harness leg that produced the exit-127 hook_non_blocking_error attachments in 228fb4d0 and 6b9a162f — now green. Regression: 5 shell-matrix wiring tests (suite 215→220) + wiring_matrix.ps1 as the re-runnable artifact; shape A (sh -c 'exec …') is the documented fallback per D2. Residual: doctor's hook probe still spawns wsl.exe directly — Phase 2 (D-010) makes probes traverse the Git-Bash consumer leg from a non-repo cwd.
### H-09 — Hook digest silently empty when executor cwd is not the repo: engine discovers the repo from cwd, wrapper does not anchor, cmd.exe cannot hold a UNC cwd

- **Severity**: medium
- **Status**: resolved (2026-06-10)
- **Affected**: clauderizer-hook repo discovery (cwd walk); the generated .clauderizer/hook.sh (inherits cwd instead of anchoring to the repo it was generated for); init's spawn-test and doctor's hook probe (neither varies cwd, so neither can catch this).
- **Invariant violated**: H-01 / L-07: the silence channel reopened below the wrapper's observable surface — the wrapper ran fine, the engine answered "no repo here" with empty/0, and that is indistinguishable in-band from a healthy hook in a repo-less directory (L-09: only the digest arriving proves the chain).
- **Preconditions**: Executor or harness whose spawn cwd differs from the project repo: cmd.exe with any UNC-rooted project (structural), or any client that spawns hooks from a fixed directory.
- **Impact**: Any harness or executor that spawns the SessionStart command without preserving the project cwd gets an empty exit-0 hook — no digest AND no breadcrumb (the wrapper runs, but the engine prints nothing and the wrapper's [ -n "$out" ] branch stays silent). Found live in Phase 0: the matrix's entire cmd.exe column was empty because cmd cannot hold the UNC project cwd (\\wsl.localhost\...) and falls back to C:\Windows; reproduces equally from PowerShell with cwd C:\ and from the engine directly with --cd /tmp. A future harness spawning hooks from the user home would silently kill the digest on every host.
- **Root cause**: The engine hook resolves its repo from the inherited working directory, and the generated wrapper — which is per-repo and knows its repo path statically (its own engine-hook path is already hardcoded) — does not cd to that repo before delegating.
- **Reproduction**: wsl.exe -d ubuntu --cd /tmp /home/ccusce/Clauderizer/.venv/bin/clauderizer-hook → 0 bytes, exit 0. cmd /d /c "wsl.exe -d ubuntu /bin/sh /home/ccusce/Clauderizer/.clauderizer/hook.sh" with UNC cwd → 0 bytes on BOTH streams (file-redirect verified), exit 0. Control: an anchored variant (cd /home/ccusce/Clauderizer before delegating; /tmp/hook-anchored.sh) emits the full 758-byte digest from cwd C:\ under all three executors.
- **Recommended fix**: Phase 1: hosts.py generates a self-anchoring wrapper (cd <repo> || exit 0 before invoking the engine hook — keep exit 0 so a deleted repo stays a silent no-op for the harness, or emit a breadcrumb naming the missing repo). Phase 2: init's spawn-test and doctor's hook probe must spawn from a NON-repo cwd so anchoring is what they verify. scripts/wiring_matrix.ps1 already runs hostile-cwd (C:\) by default.
- **Regression tests**: Owed Phase 1 (generated wrapper contains the anchor; unit test on template output) and Phase 2 (probe spawns from non-repo cwd).
- **Resolution**: 2026-06-10, Phase 1 of harness-truth-and-release-ritual: render_hook_wrapper now anchors the generated wrapper to its repo (cd '<repo>' 2>/dev/null with a REPO_BREADCRUMB_PREFIX stdout breadcrumb on failure, exit 0; cmd variant anchors with cd /d). Evidence: 5 new tests including executing the anchored wrapper from a foreign cwd (engine sees the repo as cwd) and the unreachable-repo breadcrumb (stdout, stderr clean); the regenerated PRODUCTION wiring passes the executor matrix from hostile cwd C:\ under all three executors (scripts/wiring_matrix.ps1, shape C row = the registered command verbatim); doctor freshness now compares full wrapper content and nudges template-outdated wrappers (exit 3). Suite 220 green; init idempotent (second run: 0 files). Residual: Phase 2 makes init/doctor probes spawn from a non-repo cwd so regression is caught by checks, not only by the matrix script.

### H-10 — Windows: a finished writer orphans its own lock when unlink races a concurrent reader

- **Severity**: medium
- **Status**: resolved (2026-06-20)
- **Affected**: src/clauderizer/locking.py (_release_file); tests/test_locking.py::test_concurrent_writer_processes_lose_nothing
- **Invariant violated**: Extends H-05: the advisory write lock must not spuriously deny a writer when no live holder exists. No lost-update/duplicate-id occurs (takeover recovers), so this is a liveness/robustness gap, not a correctness violation.
- **Preconditions**: Windows host; two or more processes contending for one repo's .clauderizer/write.lock concurrently (waiters polling read_holder while a holder releases). POSIX is unaffected (unlink succeeds on open files).
- **Impact**: On Windows, os.unlink in locking._release_file raises a sharing violation (PermissionError) when another process has the lock file open. Waiters poll read_holder, holding the file open for an instant on each poll, so a just-finished writer's release can fail; the error was swallowed (except OSError: pass), orphaning the lock. The orphan is then recovered only by stale takeover (~stale_timeout, default 30s), so a second writer racing the same repo's write.lock gets a spurious, retryable LockHeld for up to ~30s even though the first writer already completed. Surfaced as intermittent failures of tests/test_locking.py::test_concurrent_writer_processes_lose_nothing on windows-latest CI cells (seen on py3.11 and py3.12 on different runs), which red-flagged the 0.14.1 release matrix.
- **Root cause**: _release_file caught and swallowed the unlink OSError instead of retrying. Windows refuses to delete a file with an open handle, and concurrent pollers keep one open intermittently, so the holder's single unlink attempt can lose the race and leave the file behind.
- **Reproduction**: Run tests/test_locking.py::test_concurrent_writer_processes_lose_nothing (8 writer processes) on a slow/contended windows-latest runner: intermittently a waiter raises LockHeld 'waited Ns' while the holder field shows a since-timestamp older than the wait -- an orphaned lock not recovered within the acquire budget. Originally masked by acquire_timeout (10s) < stale_timeout (30s), so takeover never fired before the waiter gave up.
- **Recommended fix**: _release_file should retry the unlink for a short window (with the nonce guard re-checked each pass) before falling back to stale takeover. Deterministic regression test that simulates a transient unlink failure. (Defense in depth: the concurrent-writer test also widens the child acquire budget far above the stale window so takeover can rescue a waiter if an orphan ever slips through.)
- **Regression tests**: tests/test_locking.py::test_release_retries_unlink_past_transient_oserror (monkeypatched unlink fails twice then succeeds; asserts retry and no orphan).
- **Resolution**: Fixed in PR #15 (branch fix/windows-lock-orphan-release-retry): locking._release_file now retries the unlink (~2s, _RELEASE_UNLINK_ATTEMPTS, with the nonce guard re-checked each pass) before falling back to stale takeover, so a finished writer no longer orphans its lock when os.unlink loses a race with a concurrent reader on Windows. POSIX returns on the first attempt. Regression test tests/test_locking.py::test_release_retries_unlink_past_transient_oserror added (monkeypatched unlink fails twice then succeeds; asserts retry + no orphan); the concurrent-writer test retains the widened acquire budget from PR #14 as a backstop. Verified: full suite green locally; CI matrix green across 3 consecutive runs (all windows cells, py3.11/3.12/3.13).

### H-11 — claude-code .mcp.json/.claude writers skip path-safety — machine-specific command leaks on commit

- **Severity**: HIGH
- **Status**: resolved (2026-06-21)
- **Affected**: src/clauderizer/scaffold/init.py _register_mcp/_register_hook (claude-code branch); contrast hosttargets.emit_mcp which gates via is_path_safe
- **Invariant violated**: D-031 (committable configs carry no machine-specific path)
- **Preconditions**: init on a venv/pipx install (the common case), OR --session-host windows-wsl:<distro>, OR --run-cmd /abs/path; then the repo's .mcp.json/.claude/settings.json get committed (init gitignored only .clauderizer/index.json).
- **Impact**: Committing a repo init'd on a venv/pipx/split-host install leaks the author's absolute home path (username) + WSL distro into .mcp.json/.claude/settings.json and ships wiring dead on every other machine. Asymmetric with the cross-host emitters, which REFUSE such commands.
- **Root cause**: is_path_safe was enforced only on the non-claude per-host emitter path (emit_mcp raises); the default claude-code branch wrote the resolved command straight to .mcp.json/.claude/settings.json with no gate, and those files were not gitignored.
- **Reproduction**: init(repo) on a venv -> .mcp.json carries the absolute venv path; init --session-host windows-wsl:ubuntu -> wsl.exe shim in both files; path_safety_audit reports them as offenders yet init wrote them.
- **Recommended fix**: RESOLVED: init now gitignores .mcp.json when its resolved command is not path-safe (the local-wiring case) — same protection the dogfood uses; a portable uvx command stays committable. Refusal was rejected because split-host wiring legitimately needs the shim. .claude/settings.json (a potentially user-shared file with an always-absolute hook path) is documented in TRUST.md and left for the user to gitignore.
- **Regression tests**: tests/test_host_target_init.py::test_init_gitignores_machine_specific_mcp_json, test_init_portable_mcp_json_stays_committable

### H-12 — A symlinked .claude/skills/clauderizer-* entry aborts uninstall mid-footprint

- **Severity**: LOW
- **Status**: resolved (2026-06-21)
- **Affected**: src/clauderizer/scaffold/uninstall.py (skill-removal loop; the .clauderizer rmtree)
- **Invariant violated**: uninstall fully reverses the footprint
- **Preconditions**: attacker pre-plants a symlink in the cloned working tree at .claude/skills/clauderizer-evil
- **Impact**: A planted symlink masquerading as a clauderizer skill makes shutil.rmtree raise (refuses to rmtree a symlink), aborting `clauderize uninstall` partway and leaving a partial footprint. No data loss (Python's rmtree guard prevents following the link), but uninstall does not complete.
- **Recommended fix**: RESOLVED: skill removal now unlinks a symlink (never follows it) and rmtree's only real dirs, each in its own try/except so one hostile entry can't abort the run; the .clauderizer dir is unlinked-not-rmtree'd if it is a symlink, and rmtree uses ignore_errors.
- **Regression tests**: tests/test_host_target_init.py::test_uninstall_handles_symlinked_skill_without_aborting

### H-13 — Engine file writes follow symlinks (a pre-planted symlink redirects an init write outside the repo)

- **Severity**: LOW
- **Status**: resolved (2026-06-23)
- **Affected**: src/clauderizer/scaffold/init.py (_register_mcp/_register_hook write_text), src/clauderizer/markdown/writer.py (all write paths use write_text, which follows symlinks)
- **Invariant violated**: init writes inside the repo only
- **Preconditions**: attacker pre-plants a symlink in the cloned working tree AND the user runs `clauderize init` on it before reviewing
- **Impact**: A pre-planted symlink (e.g. .mcp.json -> /outside/file) in a hostile cloned working tree causes init to write its config through the link to the outside path. The written content is the benign, NON-attacker-controlled clauderizer config (no arbitrary-content write), so impact is limited to writing a known config to an attacker-chosen path within the user's write permissions.
- **Recommended fix**: OPEN (deferred, low severity): add an os.path.islink guard (or O_NOFOLLOW where available; islink+refuse on Windows) before engine-owned config writes in writer.py and init's _register_* — refuse or replace-not-follow a symlinked target. Not auto-fixed now because the fix spans writer.py cross-platform and the vector requires a malicious working tree the user chose to init; tracked for a future hardening pass.
- **Resolution**: Fixed 2026-06-23 (1.1.0): markdown/writer.py adds refuse_if_symlink(), called by all three write paths (_write_if_changed, remove_marker_block, create_if_absent) and by init's _register_mcp/_register_hook config writes — a symlinked target raises OSError, never followed or deleted (the user reviews/removes the link). Cross-platform via Path.is_symlink() (inert on normal paths). Regression-tested: tests/test_engine_hardening.py::test_writer_refuses_to_write_through_a_symlink and ::test_register_mcp_refuses_a_symlinked_target (run on POSIX, skip if the platform forbids symlink creation). Suite green at 619. Residual: a symlinked PARENT directory is not caught (only the final target); the named .mcp.json/settings.json/markdown vectors are closed.
### H-14 — uvx-wired MCP server command omits the [mcp] extra → cz_* tool surface is dead on a zero-install

- **Severity**: critical
- **Status**: resolved (2026-06-23)
- **Affected**: src/clauderizer/scaffold/init.py:30 (DEFAULT_RUN) + _resolve_invocation (reuses the same uvx prefix for the MCP and hook commands); src/clauderizer/mcp_server.py:104,166-171 (lazy `from mcp.server.fastmcp import FastMCP`, graceful refusal); pyproject.toml:26,31 (core dependencies = []; mcp is [project.optional-dependencies]).
- **Preconditions**: Install/run via uvx or pipx without explicitly adding the [mcp] extra — i.e. exactly the documented `uvx --from clauderizer …` zero-install flow.
- **Impact**: On a fresh zero-install (uvx/pipx) — the documented primary install path — the entire cz_* MCP tool surface is unavailable. `clauderize init` wires the MCP server as `uvx -q --from clauderizer clauderizer-mcp`, but `mcp` is an optional extra, so `--from clauderizer` never installs it; the server prints the missing-mcp notice and exits without serving. A real Claude Code session gets the SessionStart hook digest but NO MCP tools — i.e. the core product value is silently absent. Maintainer/dev environments (editable venv with [mcp] installed) mask it entirely. Independently reproduced across all three dogfood runs (pet/standard/saas) on published 1.0.2.
- **Root cause**: DEFAULT_RUN = ['uvx','-q','--from','clauderizer'] is reused verbatim for BOTH entry points. The hook (clauderizer-hook) needs no extra, but the MCP server (clauderizer-mcp) requires the optional mcp dep, so the composed MCP command lacks [mcp].
- **Reproduction**: `uvx -q --from clauderizer clauderizer-mcp` → prints "The MCP server needs the 'mcp' package…", serves nothing. Fix-verified: `uvx -q --from "clauderizer[mcp]" clauderizer-mcp` → answers JSON-RPC initialize.
- **Recommended fix**: Wire the MCP-server uvx command with the extra — `uvx -q --from "clauderizer[mcp]" clauderizer-mcp` — for the MCP entry ONLY (hook and CLI stay `--from clauderizer`). Targeted change in _resolve_invocation's uvx fallback. (Alternative: promote mcp to a core dependency — rejected: it breaks the zero-dep CLI/hook promise in pyproject.toml:23-26.) Requires a 1.0.3 to reach users.
- **Regression tests**: Add a test asserting the uvx-fallback MCP command contains `clauderizer[mcp]` while the hook command does NOT; ideally an integration check that the [mcp] form answers `initialize`.
- **Resolution**: Verified resolved 2026-06-23 (status was stale-open). Fix shipped in 1.0.3 (commit 6bf8be6): scaffold/init.py:127 wires `clauderizer[mcp]` for the MCP server command only (hook/CLI stay `--from clauderizer`). Regression-tested: tests/test_engine_hardening.py:126-131 and tests/test_init.py:119-122 assert the MCP argv contains clauderizer[mcp]; suite green at 617.
### H-15 — clauderize doctor reports MCP server 'launchable' via a --version probe that never imports mcp → false-positive hides the dead wiring

- **Severity**: high
- **Status**: resolved (2026-06-23)
- **Affected**: src/clauderizer/cli.py:205-206 (verdict "MCP server launchable" → hosts.verify_wiring); src/clauderizer/hosts.py spawn_probe/verify_wiring (probe_arg defaults to "--version").
- **Impact**: doctor's "MCP server launchable for session host" check passes because the launchability probe runs `clauderizer-mcp --version`, which short-circuits before the lazy `from mcp…` import that real serving requires. So doctor is green (e.g. 14/15 ✓) even when the wired MCP command cannot serve (the companion finding's condition). A first-time user who trusts `clauderize doctor` to confirm setup believes the core tool surface is healthy when it is entirely dead. Independently observed in all three dogfood runs.
- **Root cause**: Launchability is certified with a --version identity probe, which the MCP entry can answer without importing the mcp SDK. The probe doesn't exercise the import path that serving depends on, so a missing [mcp] extra is invisible to doctor.
- **Reproduction**: On a uvx-only install: `clauderize doctor` → "✓ MCP server launchable"; but `uvx -q --from clauderizer clauderizer-mcp` (serve) → missing-mcp refusal.
- **Recommended fix**: Make doctor's MCP verdict exercise what serving needs: a real JSON-RPC initialize handshake, or a `clauderizer-mcp --selfcheck` that attempts the FastMCP import, or verify the wired `--from` resolves the mcp module. Pairs with the [mcp]-extra wiring fix.
- **Regression tests**: Test that doctor's MCP verdict FAILS when the wired command lacks the [mcp] extra (and passes once it's present).
- **Resolution**: Verified resolved 2026-06-23 (status was stale-open). Fix shipped in 1.0.3 (commit 6bf8be6): doctor now reports "MCP server wiring includes the [mcp] extra" and fails when missing, via cli.py `_mcp_wiring_missing_extra` (cli.py:226). Regression-tested: tests/test_engine_hardening.py:135-147 (test_doctor_flags_mcp_wiring_missing_the_mcp_extra); suite green at 617.

### H-16 — Engine writes follow a symlinked PARENT directory (the leaf-only symlink guard misses a symlinked container)

- **Severity**: low
- **Status**: resolved (2026-07-25)
- **Affected**: All engine write paths that mkdir a parent then write: src/clauderizer/markdown/writer.py, src/clauderizer/scaffold/init.py, src/clauderizer/scaffold/uninstall.py, src/clauderizer/hosttargets.py emitters.
- **Invariant violated**: Engine writes land inside the repo only.
- **Preconditions**: A hostile cloned working tree pre-plants a symlinked DIRECTORY (not just a leaf file) AND the user runs clauderize init before reviewing it.
- **Impact**: refuse_if_symlink() (the H-13 fix) checks only the final path component. A pre-planted symlinked PARENT directory (e.g. .clauderizer -> /tmp/evil, or .cursor -> outside) is not caught: path.parent.mkdir(exist_ok=True) is a no-op on an existing symlinked dir, so the subsequent write lands inside the link target, outside the repo. Same limited impact class as H-13 — the engine writes only its own NON-attacker-controlled config/templates; no arbitrary-content write.
- **Root cause**: refuse_if_symlink(path) checks path.is_symlink() (the leaf), not whether any ancestor up to the repo root is a symlink.
- **Recommended fix**: DEFERRED (low impact + real compatibility cost). Blanket-refusing a symlinked parent (e.g. `if path.parent.is_symlink(): raise`) would BREAK legitimate setups where a user symlinks docs/ or a config dir to a shared/synced location. The safe fix is repo-root containment — resolve() the final path and assert it stays under the repo root — which needs the repo root threaded through the write calls. Tracked for a dedicated hardening pass. All LEAF-symlink vectors (the documented H-13 cases, across writer.py / init.py / uninstall.py / hosttargets.py) are closed and regression-tested.
- **Resolution**: Resolved in Phase 3 of the 1.14.2 backlog gameplan. refuse_if_symlink now walks the whole ancestor chain, not just the leaf. Demonstrated as a real escape rather than argued: on the pre-fix tree, with docs -> /tmp/outside planted, write_atomic WROTE THROUGH IT and the file landed outside the repo, while every file under that directory is a perfectly ordinary non-symlink so the leaf guard passed cleanly. Post-fix the same input is refused and the destination stays empty -- asserted in both directions per L-29 (prove the isolation, never assume it), including through append_to_section so the guard holds at the layer mutations actually use rather than only when write_atomic is called directly. The error names the symlinked DIRECTORY and explains why the leaf looked clean, since the useful remedy is removing that link. Existing parents only, and the ordinary-write path is pinned against a false positive because this now walks to the filesystem root on every write. 6 tests.
### H-17 — Preflight test command exits 127 when the engine runs from a different env than the project venv

- **Severity**: medium
- **Status**: resolved (2026-06-25)
- **Affected**: src/clauderizer/rituals/preflight.py (_command_env / _default_runner)
- **Preconditions**: Engine launched from a non-project environment (uvx/pipx/global) AND the project keeps its test runner in a local .venv that is not on the inherited PATH.
- **Impact**: cz_preflight's tests gate exits 127 ('pytest: not found') and cannot capture a baseline on any host where the MCP server/engine is launched from a SEPARATE environment (uvx, pipx, global) than the project's venv — a common new-adopter setup (uvx clauderize init). The core pre-flight gate is effectively unusable there and blocks the gameplan workflow, even though the suite is green via the project venv.
- **Root cause**: _command_env prepended only the ENGINE interpreter's bin (Path(sys.executable).parent) — which fixes the editable-venv-launched engine (A-001) but not the case where the engine's environment differs from the project's venv. The project's own .venv/bin was never added to PATH. A fresh instance of L-23: the author's editable-venv setup never exercises the shipped uvx path.
- **Reproduction**: Launch the MCP server via uvx (engine env has clauderizer but not pytest); project has .venv/bin/pytest; run cz_preflight -> tests check exits 127 while `.venv/bin/python -m pytest` is green.
- **Recommended fix**: _command_env(root) now also detects project-local virtualenv bin dirs (.venv/venv x bin/Scripts) and leads PATH with them BEFORE the engine bin; _default_runner passes cwd. Cross-platform, no template hardcode, and only prepends a detected venv so users who already have the runner on PATH are unaffected.
- **Regression tests**: tests/test_structural_writes.py::test_command_env_prepends_project_venv_bin, ::test_command_env_finds_project_venv_when_engine_is_elsewhere (monkeypatches sys.executable to a non-venv path), ::test_command_env_without_project_venv_keeps_engine_bin_first
- **Resolution**: Fixed on branch fix/preflight-venv-detection: _command_env(root) prepends project-local venv bin dirs (.venv/venv x bin/Scripts) ahead of the engine bin; _default_runner passes cwd. Regression tests green (suite 626 -> 629), incl. the monkeypatch test reproducing the uvx case (engine elsewhere, project .venv leads PATH).

### H-18 — Obsolete-lesson marker parser miscounts a lesson as active when its reason contains a ')'

- **Severity**: low
- **Status**: resolved (2026-07-16)
- **Affected**: the obsolete-marker detection used by cz_corpus_health / cz_status project-lesson counting and the handoff roll-up prune (marker regex for `(obsolete <date>: <reason>)`)
- **Impact**: A lesson obsoleted with a reason string containing a close-paren ')' (e.g. a nested parenthetical) is not recognized as obsolete by the corpus-health / status lesson counter, so it is counted active AND keeps rolling into every future handoff — silently defeating the obsoletion and re-inflating handoff size. Memory-correctness bug, not a security issue.
- **Root cause**: The marker matcher appears to require no ')' between '(obsolete' and the closing paren (e.g. an anchored `\(obsolete[^)]*\)` or line-suffix match). Reasons with an internal ')' — from nested parentheticals — break the match, so the line is read as non-obsolete.
- **Reproduction**: cz_obsolete_lesson(number='L-43', reason='situation-specific (parked-branch resume); ...') on docs/LESSONS.md; then cz_corpus_health reports active_project_lessons one higher than the true active count and lists L-43 in never_surfaced_ids despite the '(obsolete ...)' marker being present. Removing the inner parens from the reason and reindexing drops the count to the correct value.
- **Recommended fix**: Make obsolete-marker detection tolerant of ')' inside the reason: anchor on the '(obsolete ' prefix (presence-based) rather than requiring a paren-free reason, or balance-match to the final ')'. Add a regression test with an obsolete reason containing nested parentheses.
- **Regression tests**: Add a test: obsolete a project lesson with a reason containing '()' and assert corpus-health active count decrements and the handoff roll-up prunes it.
- **Resolution**: Fixed in lesson_state.py _STATE_RE: payload now tolerates one level of nested parentheses while keeping the end-anchor and post-keyword word boundary, so an obsolete/promoted reason containing parens parses as the marker instead of reading the line as active. Regression test added in tests/test_lesson_state.py::test_marker_reason_with_parentheses_parses. Suite 821 to 822 green. Ships in 1.8.1.

### H-19 — cz_audit certifies "version single-sourced" from three LOCAL sources only — a version can be locally consistent and nonexistent on every remote registry

- **Severity**: medium
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/rituals/audit.py (_pyproject_version, _dunder_version, _changelog_top_version — the version single-sourcing check); the gameplan-close ritual that runs cz_audit (D-051); clauderizer-close-gameplan SKILL.md step 5, which presents the audit as the release-readiness gate.
- **Invariant violated**: INVARIANT-07 — the released cz_* tool surface is what Claude Code parity is measured against; an unpublished tool surface means the parity claim is unverified for every consumer who is not the author.
- **Impact**: A gameplan closes clean, the audit gate reports version single-sourcing satisfied, and the release never happens — silently. Measured instance: 1.13.0 is set in pyproject.toml, src/clauderizer/__init__.py and the top CHANGELOG entry (all three agree, so cz_audit passes), while git tags local and remote stop at v1.12.0, the newest GitHub Release is 1.12.0 (2026-07-19), and PyPI's latest is 1.12.0. The dreaming-loop feature and its five tools (cz_add_dream, cz_dream, cz_dream_propose, cz_handle_dream_proposal, cz_register_dream_schedule) have never been published, yet commit 81a99f4 is titled "ship 1.13.0" and two further commits were built on top of it. Downstream: every consumer resolving `uvx --from clauderizer[mcp]` gets 1.12.0 with 62 tools while the repo's own hook-written digest advertises 67 and CLAUDE.md mandates cz_add_dream — an instruction no released engine can satisfy.
- **Root cause**: L-51 sweep (1) names FOUR registries that never sync — source, remote git tags, GitHub Releases, the PyPI index. cz_audit, the gate that runs automatically at every gameplan close, checks three sources that all live on the same machine and are usually edited in the same commit, so they agree by construction and the check is close to tautological. The four-registry sweep DOES exist, correctly implemented in src/clauderizer/release_check.py (_pypi_claimed, ls-remote --tags, the Releases sweep, and the origin/main-before-tag ordering invariant) — but it is a separate `clauderize release-check` command that nothing in the close ritual invokes. The gate that runs is the weak one; the strong one has to be remembered, which is the failure mode this project exists to eliminate.</root_cause>
<parameter name="reproduction">Non-destructive, read-only: (1) `grep -m1 '^version' pyproject.toml; grep __version__ src/clauderizer/__init__.py; grep -m1 '^## ' CHANGELOG.md` -> all 1.13.0. (2) `git ls-remote --tags origin | grep v1.1` -> stops at v1.12.0. (3) `gh release list --limit 5` -> newest 1.12.0. (4) `python -c "import json,urllib.request; d=json.load(urllib.request.urlopen('https://pypi.org/pypi/clauderizer/json')); print(d['info']['version'], '1.13.0' in d['releases'])"` -> 1.12.0 False. (5) cz_audit reports no version finding.
- **Recommended fix**: Have the close-out ritual invoke the four-registry sweep that already exists rather than the local-only one: cz_audit calls into release_check for the remote legs (network-optional — an unreachable registry reports "unverified", never a false green, per L-25 capability-not-presence), or the close-gameplan skill requires `clauderize release-check` for any release-bearing gameplan. Distinguish the two verdicts explicitly in the output: "locally consistent" is not "released". Additionally: an unreleased source version is the NORMAL dogfood state, so surface it as a one-line status signal ("source 1.13.0 is ahead of the latest release 1.12.0") rather than treating it as an error — that single line would have prevented this finding, D-060's engine split, and the uncompliable cz_add_dream instruction.
- **Regression tests**: A test asserting the close-out audit reports a finding when the local trio agrees but the remote legs do not (fixture-mocked registries — no live network in the suite); a test that an unreachable registry yields "unverified" rather than pass; and a doc-pin test that clauderizer-close-gameplan's step 5 names whichever gate actually sweeps four registries.
- **Resolution**: Resolved in 1.14.0 Phase 6 with this finding's own recommended fix. cz_audit's _release_signals now sweeps the three REMOTE registries in addition to the three local sources, via release_check.remote_claims (remote git tag, GitHub Release, PyPI). The local trio agree by construction because one commit edits all three, which is why the gate passed on 1.13.0 while that version existed on zero remote registries — a release sitting unshipped behind a commit titled 'ship 1.13.0'. Network-optional per L-25: an unreachable registry reports 'unverified' and never a false green. Verified live: remote_claims reports 1.13.0 claimed on all three and 1.14.0 on none. Three fixture-mocked tests (no live network in the suite) cover flagged / silent / unverified, and a CLAUDERIZER_NO_NETWORK seam keeps the unit suite off the network, mirroring the spawn-probe seam.
### H-20 — The D-032 release gate was decided as launch-and-round-trip and implemented as a substring match — the docs described the decision, the code never caught up

- **Severity**: medium
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/hosttargets.py verify_emitted_wiring and wiring_contract_sweep; its only test in tests/test_hosttargets.py asserts every host returns True; docs/CROSS-HOST.md section 7 (corrected 2026-07-24 to describe the implementation rather than the decision); docs/DECISIONS.md D-032 (left untouched — append-only, INVARIANT-03).
- **Invariant violated**: Related to INVARIANT-07 (Claude Code parity is a release blocker) — a release gate that cannot detect a non-launching config cannot protect that parity.
- **Impact**: D-032 records the decision that the automated release gate verifies "the MCP server launches, and an in-process host-simulator stub round-trips cz_status". The shipped implementation, hosttargets.verify_emitted_wiring, parses the emitted JSON, checks the entry has a command, checks path-safety, and then checks that some token in argv contains the string "clauderizer-mcp". It spawns nothing, connects nothing, and round-trips nothing. A config naming a command that does not exist passes for all 11 auto-write hosts. docs/CROSS-HOST.md faithfully described the DECISION, so the doc was not lying — the code diverged from the decision and nobody compared them. Consequence: the release gate that CROSS-HOST.md rests the Beta claim on cannot fail for the reason it exists, and the same class of false green (presence rather than capability) that L-25 was promoted to prevent is the shipped default on the primary path.
- **Root cause**: A decision that specifies a verification MECHANISM creates an obligation on code that nothing checks. There is no test asserting that the gate does what D-032 says, and no review step comparing a decision's stated mechanism against its implementation. This is the same seam L-55 names for docs and code, applied one level up: decisions drift from implementations exactly as prose drifts from code, and the drift is invisible because both sides read as correct in isolation. Related instances found in the same audit: H-06 and H-15 (launchability certified as identity) were recorded and marked resolved while the native doctor path still checks only PATH presence.
- **Reproduction**: Read src/clauderizer/hosttargets.py verify_emitted_wiring: after json.loads and the is_path_safe check it returns True on any argv where some token contains the substring clauderizer-mcp. Compare against docs/DECISIONS.md D-032 which specifies a launching host-simulator that round-trips cz_status. No subprocess is spawned anywhere in that function or its caller.
- **Recommended fix**: Either implement D-032 as decided — spawn each emitted argv through mcp_probe.handshake_probe and issue one tools/list, which is the same primitive the bespoke-host path already uses — or record a superseding decision that narrows the gate to a static shape check and says why. Do not leave a decision and its implementation disagreeing silently. Generalize the guard: when a decision names a verifiable mechanism, the phase that implements it owes a test that would fail if the mechanism were replaced by a weaker one.
- **Regression tests**: A test asserting wiring_contract_sweep fails for a host whose emitted config names a nonexistent command (today it passes). A test asserting the gate completes an initialize handshake and receives serverInfo.name == clauderizer for at least one emitted config. A doc-pin test that CROSS-HOST.md section 7's description matches whichever behavior actually ships.
- **Resolution**: Resolved in 1.14.0 Phase 4 with this finding's own recommended fix and its three recorded regression tests. hosttargets.verify_emitted_wiring now completes an MCP initialize handshake asserting serverInfo.name instead of `any(clauderizer-mcp in tok)`; a config naming a non-existent command now FAILS the contract where it previously passed for all eleven auto-write hosts. doctor additionally verifies engine IDENTITY on the DEFAULT path for the portable .mcp.json — previously routed to shutil.which — reporting `initialize -> serverInfo clauderizer 1.13.0` and warning (exit 3, never a pass, never a failure) on a served-vs-source version skew. The handshake is memoized on (command, args) so nine identical auto-write entries collapse to one spawn; measured cost warm 1.0s / cold-cache 2.7s against an 8s budget. Three-state contract preserved: unverifiable is reported by name and never rendered green or as a failure. CROSS-HOST section 7 was corrected to describe what ships, pinned by a claim test.
### H-21 — A superseded gameplan cannot be closed — the procedure's close ritual says complete-or-deferred, but no deferred phase state exists so lifecycle can never reach complete

- **Severity**: low
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/rituals/status_bundle.py _lifecycle and gameplan_card (open is derived as lifecycle != complete, and lifecycle is complete only when every phase row reads complete); src/clauderizer/mutations.py transition_phase (the accepted normalized states are not_started, in_progress, blocked, complete, failed — there is no deferred or superseded); docs/gameplans/GAMEPLAN-PROCEDURE.md Procedure: Close a Gameplan, step 1, which requires "all phases complete or explicitly deferred"; src/clauderizer/skills/clauderizer-close-gameplan/SKILL.md step 1, which repeats it.
- **Impact**: A gameplan abandoned or superseded before execution stays open in the portfolio forever, or is closed by marking its phases complete — which is a false record of work that never happened. The portfolio digest therefore accumulates gameplans that will never be worked, diluting the one signal it exists to give. Concrete instance: 2026-07-24-workflow-critique-repairs-engine-identity-ranker-length-bias-cascade-yield-saturated-telemetry was superseded on the day it was planned, has a full POST-MORTEM recording the supersession, and still reports as open with phase 0 of 6 ready. This repo now shows six open gameplans, of which at least two will never be executed as written.
- **Root cause**: The procedure specified a disposition the engine never implemented. "Explicitly deferred" appears in the close ritual and in the status-value table as a first-class entity status, but the phase-row vocabulary has no equivalent, and the portfolio's open/closed derivation reads only phase rows. So the one path the procedure names for closing an unfinished gameplan is unreachable. This is the same class the whole 1.14.0 release addresses (D-065): a written intent with no executable check, where the code and the decision disagree silently and both read as correct in isolation.
- **Reproduction**: Read status_bundle.py: open is lifecycle != complete, and _lifecycle returns complete only when all phase rows are complete. Read mutations.py's phase-state map: no deferred entry. Then run clauderize status on this repo and observe the superseded gameplan listed as open at phase 0 of 6 despite carrying POST-MORTEM.md.
- **Recommended fix**: Add a terminal deferred or superseded phase state and teach _lifecycle to treat a gameplan whose phases are all terminal — complete, deferred, or superseded — as closed, with the portfolio distinguishing shipped from abandoned. A superseded gameplan should carry a pointer to its successor. Until then the honest disposition is to leave it open with its POST-MORTEM rather than marking unexecuted phases complete, and to say so in the digest. Do NOT resolve this by marking phases complete: L-38 already covers exactly this — record the honest amendment rather than faking the checkbox.
- **Regression tests**: A test asserting a gameplan whose phases are all deferred reports lifecycle closed and drops out of the portfolio's open count. A test asserting the portfolio distinguishes a completed gameplan from a superseded one. A doc-pin test that the close-gameplan skill and GAMEPLAN-PROCEDURE's close ritual name only dispositions the engine can actually reach.
- **Resolution**: Resolved in Phase 3 of the 1.14.2 backlog gameplan. The phase-row vocabulary gains DEFERRED/SUPERSEDED/ABANDONED/WONTFIX -> 'deferred', matched BEFORE GATED/BLOCKED so 'DEFERRED (blocked on upstream)' does not silently degrade back into the open set. Threaded through all three surfaces that must agree: _lifecycle (all-deferred reports 'deferred' rather than 'complete', so a dropped gameplan is closed without ever claiming work that was not done; finished-or-dropped counts as complete), gameplan_card's open flag (open = lifecycle not in complete/deferred), and the completed-gameplan digest branch. RED on the pre-fix tree: 'DEFERRED' parsed as 'unknown' and an all-deferred gameplan reported lifecycle 'planning' -- i.e. permanently open, which was the finding's actual symptom. 13 tests including no-regression coverage for every pre-existing status and the INCOMPLETE word-boundary guard.
### H-22 — Nothing detects that the memory has fallen behind the work — the ending protocol is a convention, and conventions rot

- **Severity**: high
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/skills/clauderizer-do-phase/SKILL.md (the closing protocol, step 4 — instructions only); src/clauderizer/rituals/status_bundle.py (the digest, which reports phase state but never compares it to repo state); the ending protocol block rendered into every handoff by rituals/handoff.py. There is no code path anywhere that observes the divergence.
- **Invariant violated**: Relates to D-065 (no output asserts a property from evidence it never read) — the digest reports phase state from the tracker alone, never checking the repo that would contradict it.
- **Impact**: A session can implement an entire phase, commit it, push it, and move on to the next phase while the gameplan still reads that phase as not-started, with no outputs, no summary, and no handoff. Nothing in the engine notices or says so. If that session ends there, the next one inherits a corpus that describes work that no longer matches the repo — the precise cross-session amnesia this product exists to prevent, produced by the product's own workflow. Measured live in the gameplan that shipped this release: phases 5 and 6 were fully implemented, tested and pushed across eight commits while both still read as ready/not-started; the close-out only happened because the user announced they were switching sessions and asked whether a fresh one could resume. Had they simply closed the terminal, the next session would have seen phase 5 pending and a repo where phase 5 and 6 were already done — and would plausibly have re-implemented them or, worse, published a release it did not know was staged.
- **Root cause**: The ending protocol is prose in a skill, and the engine has a detector for every OTHER discipline it cares about: cascade_hygiene surfaces unresolved cascade reports, transition_phase surfaces unchecked exit criteria and unresolved open items, corpus_health surfaces redundancy, doctor surfaces wiring drift. The one discipline with no detector is the one that keeps the memory current — so the system can tell you your cascade is unresolved but not that your last three phases were never recorded. This is D-065's thesis turned inward: the digest asserts a phase state from the tracker without ever reading the evidence (commits, working tree, file mtimes) that would contradict it.
- **Reproduction**: In this repo: git log --oneline shows the Phase 5 implementation landing at eac1c9a and Phase 6 at e33e5cb, while cz_status reported the gameplan at phase 5 ready and phase 6 not-started until ef196aa, eight commits later. No tool emitted any signal in between.
- **Recommended fix**: Add a memory-lag signal to the status digest and to preflight, advisory and never blocking (INVARIANT-05). The cheapest honest version: compare the commit count (or latest commit timestamp) touching non-docs paths against the last phase-state write recorded for the focus gameplan, and surface a line when work has landed since the phase was last updated — "N commits since phase 5 was last recorded; if that phase is done, run the ending protocol". A stronger version keys on the phase's own exit criteria: if criteria naming test files or modules are satisfiable by the current tree while the phase is still not-started, say so. Both are D-065-shaped: derive the claim from evidence actually read rather than from the tracker asserting itself.
- **Regression tests**: A test asserting the digest emits a memory-lag line when the focus gameplan's current phase has non-docs commits after its last recorded state change, and stays silent when it does not. A test asserting the line never blocks and adds zero bytes when there is no lag (INVARIANT-08).
- **Resolution**: Shipped in Phase 0 of gameplan 2026-07-25 (commit 61317f5). rituals/memory_lag.py derives the claim from git, never from the tracker asserting itself (D-065): the anchor is the last commit that TOUCHED the phase tracker, work is commits past it outside the docs tree and .clauderizer/, and lag is work landing while the tracker still reads the phase unstarted. Surfaced as a conditionally-emitted digest line and an identically-worded cz_preflight warn that never fails the ritual (INVARIANT-05). Evidence: 11 tests, demonstrated red on the pre-1.14.1 tree at efdf210 (the old engine reported a clean 'preflight PASS' with a src/ commit sitting past a NOT STARTED phase); the historical test replays this repo's own eac1c9a and asserts it fires; and it fired live on its own commit before this close. Honest scope limit: a phase left marked in_progress long after it was really finished is NOT detected, because git cannot distinguish 'still working' from 'done but unrecorded'.
### H-23 — Nested clauderized repos fire two SessionStart digests, and the outer one reports "No active gameplan" about the repo you are working in

- **Severity**: high
- **Status**: resolved (2026-07-25)
- **Invariant violated**: INVARIANT-08 — cross-host status injection reaches the model at most once per session. Nesting defeats it, because the dedup signal is per-process by design.
- **Impact**: INVARIANT-08 requires status injection to reach the model at most ONCE per session. When a clauderized repo sits inside another clauderized repo, two SessionStart hooks fire and the model receives two contradictory digests. Observed live and verified: this session opened with "[Clauderizer] No active gameplan. cz_create_gameplan to start." immediately followed by the real portfolio digest for a repo mid-release. The FIRST thing an agent reads is therefore a false statement about the repo it is in — and an agent that trusts it may create a duplicate gameplan, or conclude there is no work in flight. A second session hit exactly this and had to reconcile the two before it could report state. The same nesting duplicates the CLAUDE.md stanza (both copies are loaded, and the outer one silently rots because `clauderize upgrade` only refreshes the repo it is run in — the outer copy here was a release behind, missing the cz_add_dream bullet).</impact>
<parameter name="affected">.claude/settings.json in each nested repo (one SessionStart hook each, both valid); the harness loads project settings for the cwd repo and the ancestor; src/clauderizer/hook/dispatch.py and the session-scoped dedup signal, which is per-process and therefore cannot see a sibling hook's injection; hosttargets.emit_instructions, which writes a CLAUDE.md stanza per repo with no ancestor check. Concrete instance: /home/ccusce is a clauderized git repo containing /home/ccusce/Clauderizer, also clauderized.
- **Root cause**: The at-most-once guarantee is enforced with an in-memory, session-scoped signal inside ONE engine process (INVARIANT-08 deliberately forbids a persisted flag, per INVARIANT-05). Two nested repos mean two independent hook invocations in two processes, so neither can observe the other's injection and the dedup is structurally unable to fire. Nesting was never modeled: init has no notion of an ancestor install, and doctor does not look upward.
- **Reproduction**: ls -d /home/ccusce/.clauderizer /home/ccusce/Clauderizer/.clauderizer — both exist, both are git repos. Open a session in the inner repo and read the SessionStart output: two digests arrive, the outer reporting no active gameplan. Verified independently by a second session, which flagged "Two contradictory session digests fired" before it could report state.
- **Recommended fix**: Make the engine aware of ancestry rather than trying to dedup across processes. (1) The hook should walk up from its anchored repo root and, if an ancestor .clauderizer exists, emit nothing when it is NOT the innermost install — the innermost repo is the one the session is actually working in. (2) doctor should report a nested install by name, since the outer stanza and wiring rot invisibly. (3) init should warn when clauderizing inside an existing clauderized repo and ask which is intended. All advisory, all read-only, hooks stay exit-0 (INVARIANT-06). A cheap first step is (2) alone: naming the condition costs nothing and turns a silent contradiction into a diagnosable one.
- **Regression tests**: A test asserting that with a clauderized ancestor present the inner repo's hook emits the digest and the outer emits nothing. A test asserting doctor names a detected nested install. A test asserting init warns rather than silently creating a second install inside an existing one.
- **Resolution**: Shipped in Phase 1 of gameplan 2026-07-25 (commit 439dd1b). src/clauderizer/nesting.py resolves the contradiction by OWNERSHIP rather than by a flag: for any session cwd exactly one install is the owner (its nearest clauderized ancestor), and a non-owner stays silent. Decided fresh from the hook payload cwd on every event and threaded through every handler, so nothing is persisted or shared across processes (INVARIANT-05/08) and hooks stay read-only and exit 0 (INVARIANT-06, asserted end-to-end through the real dispatcher). Scope is deliberately narrow to protect INVARIANT-07: an install falls silent ONLY when the session owner is a proper descendant of its own root, so a session elsewhere on the machine, or a host that sends no cwd, behaves exactly as 1.14.0. doctor now names nested installs by path and, from inside, the clauderized ancestor; init warns and proceeds. Evidence: 16 tests; substantive RED on the pre-1.14.1 tree at efdf210 (the old engine emitted TWO digests for a session in the inner repo, one per install); and live on this machine the outer install running the published 1.14.0 still emits "[Clauderizer] No active gameplan." for a session in /home/ccusce/Clauderizer while the same payload against the 1.14.1 engine is silent with exit 0 and the inner install alone speaks. The live scan found TEN nested installs under /home/ccusce in 0.24s. REMAINING DEPLOYMENT GAP: the outer install's hook runs the published engine via `uvx --from clauderizer`, so this repair reaches it only when 1.14.1 publishes (Phase 4) and its uvx cache refreshes.

### H-24 — Subsystem docs have no executable seam against the module they describe

- **Severity**: low
- **Status**: resolved (2026-07-25)
- **Affected**: docs/subsystems/*.md (all eight); the L-62 seam tests in tests/test_blessed_surfaces.py and the procedure-version parity test
- **Impact**: A doc that enumerates a code-owned surface drifts silently and reads as true until someone happens to look. Measured instance: the D-066 write boundary (_safe_body / _safe_cell / _one_line, the forged-heading escape, the normalize-never-reject contract) shipped in 1.14.0 and docs/subsystems/mutations.md never mentioned any of it. It was found a full release later, by accident, while writing an adjacent paragraph for a different change — not by any check. Anyone reading that doc to understand the write path would have concluded no such boundary existed.
- **Root cause**: L-62 established the executable-seam discipline — a test that diffs a doc against its source of truth — and it was applied to the README's MCP tool list and the procedure-version header. It was never generalized to docs/subsystems/*.md, which are exactly the docs whose job is to enumerate a module's surface. So the class of drift L-62 exists to kill remains live in the place it matters most.
- **Reproduction**: Read-only: `git log -S'_safe_body' --oneline -- src/clauderizer/mutations.py` shows the boundary landing in 1.14.0; `git log --oneline -- docs/subsystems/mutations.md` shows no corresponding doc change until 1.14.1.
- **Recommended fix**: Add a test asserting that each docs/subsystems/&lt;name&gt;.md mentions the load-bearing public callables of its module — derived from the source rather than hand-listed, so the assertion cannot itself go stale. Start advisory (report the gap) rather than strict, since a subsystem doc is prose and need not name every private helper; the goal is that a whole documented CONTRACT cannot go missing.
- **Resolution**: Resolved in Phase 4 of the 1.14.2 backlog gameplan, narrower than the criterion asked and honestly so (amendment A-001). tests/test_subsystem_doc_seam.py derives each subsystem's public surface from the SOURCE via ast and diffs it against the doc, then gates on two strict ratchets: undocumented callables per subsystem may only decrease, and the set of modules with no subsystem doc may only shrink. The second exists because the first alone covers just 8 of ~40 modules -- nesting.py and engine_identity.py, both written during this release, sat in that blind spot. Both gates were demonstrated firing: a new module fails with 'no subsystem doc and no recorded exemption', a new public callable fails with 'now omits 35, up from 34'. The map itself is guarded too -- a subsystem doc with no resolvable module, a stale alias, or a subsystem missing from the baseline all fail. WHAT IT DOES NOT DO, stated in the module docstring rather than implied: it cannot catch the D-066 case that motivated the finding, because _safe_body is private and the gap was a conceptual section; no mechanical check separates 'this doc discusses the right ideas' from 'this doc is stale'. Frozen debt at close: graph 13, markdown-core 19, mutations 9, profiles 1, rituals 34, scaffold 3, mcp-server 0, plus 32 unmapped modules. 19 tests.
### H-25 — Gameplan planning surfaces no lessons, so lessons about planning never reach planning

- **Severity**: medium
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/rituals/handoff.py (rank_relevant, the surfacing call site); cz_create_gameplan in src/clauderizer/ops.py; .claude/skills/clauderizer-new-gameplan and its src/ template; L-11, L-24, L-52, L-56, L-57 (the five never-surfaced project lessons)
- **Impact**: Lessons that govern how to PLAN — declare dependencies by technical need, size phases, write machine-checkable exit criteria — are structurally unreachable at the one moment they apply. Telemetry corroborates the mechanism rather than blaming any author: L-11 ("declare phase dependencies by technical need, not narrative order") has surfaced_count 0, having never reached any phase of any gameplan, and the very next plan violated it. The 1.14.1 gameplan declared 1 depends on 0, 2 on 1, 3 on 0, while phases 0-3 touch disjoint modules and were fully independent; nothing was harmed only because a single session executed them in order, but no second agent could have parallelized any of it. Four other lessons share the never-surfaced status (L-24, L-52, L-56, L-57), and the digest has been asking for a re-distill on the grounds that 25 project lessons is too many — a judgment that is unsound while a subset of them has never been given a chance to be useful.
- **Root cause**: Lesson surfacing lives in rituals/handoff.assemble, which runs PER PHASE and ranks lessons against that phase's text. cz_create_gameplan has no equivalent step, so the ranker is never invoked at plan time and the planning agent sees no lessons at all. The relevance ranker itself is fine; it is simply not wired to the planning surface.
- **Reproduction**: Read-only: cz_dream's lesson_flags (or cz_lesson_health) reports L-11 with surfaced_count 0 and last_surfaced null, while the 2026-07-25 gameplan's Phase Breakdown declares narrative dependencies for four independent phases.
- **Recommended fix**: Surface ranked project lessons at plan time, against the gameplan GOAL text, the way handoff.assemble does against phase text — read-only and advisory (INVARIANT-05), a pointer not an authority (D-013). Two consequences worth having: planning lessons finally reach planning, and the never-surfaced set stops being an artifact of where the ranker happens to be wired, which makes any future obsoletion judgment about those lessons honest.
- **Resolution**: Resolved in Phase 1 of the 1.14.2 backlog gameplan. rituals/handoff.plan_lessons ranks the project corpus against a gameplan's GOAL text using the SAME deterministic keyword + entity-id ranker the phase path uses (no ML, D-018), and cz_create_gameplan calls it with the name plus first phase -- the goal text actually available at plan time. Returned as result['relevant_lessons'] plus an advisory that says plainly they are pointers into docs/LESSONS.md and not instructions (D-013/INVARIANT-05); it never blocks or edits the plan, and a missing corpus or unusable goal returns [] rather than raising, because a plan must never fail over its own advice. The surfacing is logged through the same blessed telemetry.record_surfaced the handoff uses, with phase='plan', so lesson-utility scoring now sees plan-time surfacings and a future never-surfaced judgment is sound -- which was the compounding half of this finding, since the zero was being read as evidence of low value when it was an artifact of where the ranker was wired. 7 tests; behavioral RED on the pre-1.14.2 tree, where the same on-topic corpus produced no lesson keys in the result and no plan-time telemetry at all.
### H-26 — The lesson-bloat nudge fires on COUNT while the cost it names is TOKENS — consolidation optimizes the wrong one

- **Severity**: medium
- **Status**: resolved (2026-07-25)
- **Impact**: The digest warns "N project lessons (> 20) — docs/LESSONS.md rides in every handoff across gameplans" and prescribes consolidation. The threshold is a COUNT; the cost in the same sentence is TOKENS. They are not the same lever, and following the nudge can move them in opposite directions. Measured on this repo, 2026-07-25: a coverage-gated thematic re-distill took active project lessons 26 -> 20 (-6, clearing the warning) while total active characters went 24093 -> 24352 (+1.1%) and the estimated handoff went ~11.2k -> ~12.8k tokens (+14%). Mean lesson length rose 926 -> 1217 chars. So the warning was silenced by work that made the thing it warns about slightly worse. An agent following the nudge in good faith will keep doing this, and each pass raises the mean length, because a faithful synthesis must carry the union of its sources' distinctive content.</impact>
<parameter name="root_cause">Two independent causes compounding. (1) The threshold (project_lessons_warn = 20) counts entries, while handoff weight is driven by characters. (2) rituals/handoff.assemble renders the top RELEVANCE_K = 5 project lessons in FULL plus an index of the rest, so handoff size tracks the length of the five most relevant entries, not the corpus size — and consolidation systematically makes the highest-ranking entries LONGER, since a synthesis outranks its sources on any query that used to retrieve any one of them. There is also a genuine structural tension with L-26's coverage gate: the gate requires the synthesis to retrieve for each source's distinctive-token query, which forces the synthesis to retain each source's distinctive vocabulary — fidelity and brevity pull against each other by construction, and the gate correctly chooses fidelity.</root_cause>
<parameter name="affected">src/clauderizer/rituals/status_bundle.py (_memory_gauge, the project_lessons_warn nudge text); src/clauderizer/config.py (project_lessons_warn = 20); src/clauderizer/rituals/handoff.py (RELEVANCE_K = 5 full-text render); docs/LESSONS.md; L-67 (the curation lesson, which now carries this caveat)
- **Recommended fix**: Make the nudge fire on the metric it names. Compute the estimated handoff contribution of the lesson block (characters, or the existing chars//4 token estimate) and warn on THAT, with the count as a secondary detail — the gauge already computes handoff_est_tokens, so the input exists. If a lever is wanted for the tokens themselves, the effective one is a character budget on the full-render block in handoff.assemble (render fewer than RELEVANCE_K in full when they are long, keeping every other lesson in the index — D-068 keeps nothing dropped, so this stays a rendering choice, not a memory choice), NOT further consolidation. Do not tune this by lowering RELEVANCE_K blindly: D-021/D-068 make the full index load-bearing, and H-25 has already shown that five lessons never surface at all.</recommendation>
<parameter name="reproduction">Read-only, on this repo at the 2026-07-25 re-distill commit: parse docs/LESSONS.md at HEAD~1 and HEAD, count active `**L-NN.**` entries and sum their characters; compare `clauderize status`'s "Memory: … (~N tok handoff)" figure across the same two commits.
- **Resolution**: Resolved in Phase 2 of the 1.14.2 backlog gameplan. The project-lesson nudge now thresholds on PROJECT_LESSON_TOKENS_WARN (5000) against the measured token weight of the block that actually rides in every handoff, with the entry count demoted to a detail in the text; config.project_lessons_warn survives only as that secondary detail. The warning also states outright that consolidation alone may not reduce the number, because a synthesis outranks its own sources and the entries rendered in full therefore get longer -- following the old nudge blindly is what produced this finding. Verified against the very corpus that caused it: after the 1.14.1 re-distill took 26 -> 20 entries the live gauge reads ~6168 tok and STILL fires, correctly refusing to call that an improvement. Test test_consolidation_that_does_not_shrink_bytes_does_not_clear_the_warning pins the acceptance case directly. Separately, the aging half: _findings_by_age derives an open finding's age from its own recorded date and the digest names the oldest and counts everything past FINDING_STALE_DAYS (30), so a finding carried across releases no longer reads like one opened an hour ago; undated entries contribute nothing (D-065) and a young register renders byte-identically (INVARIANT-08). Live on this repo it reports H-16 at 32d. 10 tests.
### H-27 — The MCP server serves the PUBLISHED engine, so cz_* writes never run the working tree's code — and engine_stale cannot see it

- **Severity**: high
- **Status**: resolved (2026-07-25)
- **Affected**: src/clauderizer/rituals/status_bundle.py (engine_source_newer_than, PROCESS_STARTED, the engine_stale digest line); .mcp.json (portable PORTABLE_COMMAND wiring); every cz_* write path in ops.py during a session that edits the engine; docs/subsystems/mcp-server.md
- **Impact**: Every cz_* tool call in a session is served by whatever process .mcp.json launched. In this repo that is `uvx --from clauderizer[mcp] clauderizer-mcp`, i.e. the PUBLISHED package from uv's cache — not src/ in the working tree. So a fix authored this session is live for `clauderize ...` (local venv, fresh process) and for direct dispatch, while every MCP write keeps running the released build. Measured live 2026-07-25: the Phase 2 tool-call write guard was authored, tested at 26 tests and committed, and then did not execute for a single cz_* write for the rest of the session; a malformed cz_add_finding produced H-26 carrying , ,  and three <parameter name= fragments -- the FIFTH such corruption, landing in the release built to prevent it, with the guard sitting green in the working tree the whole time. The false confidence is the damage: the suite passes, the CLI demonstrates the fix, and the surface the agent actually writes through is unchanged.
- **Root cause**: Two compounding. (1) .mcp.json wires the portable published command by design (it must be machine-independent and committable), so a contributor editing the engine is served the release, not their edit -- correct for consumers, invisible for dogfooding. (2) status_bundle.engine_source_newer_than compares source mtimes against PROCESS_STARTED, which detects only the EDITABLE-install case; its own docstring notes that an installed package's mtimes are install-time and therefore never newer. A uvx-served server thus reports engine_stale=false with total confidence while running a different build entirely. The detector answers 'did my files change since I started', when the question that matters is 'am I the build the working tree describes'.
- **Reproduction**: Read-only: `ps -eo lstart,cmd | grep clauderizer-mcp` shows the served command resolving under ~/.cache/uv/archive-v0/...; compare `clauderize --version` (local venv) against the serverInfo version from an MCP initialize handshake, and call cz_status for engine_stale, which reports false in exactly this situation.
- **Recommended fix**: Make the identity check compare BUILD IDENTITY, not mtimes. doctor already completes an MCP initialize handshake asserting serverInfo.name and version (H-20); reuse it: when the served version differs from the working tree's __version__, or the served module path is not under this repo, say so in the digest -- that is the same capability-not-presence rule as L-25 and it catches the uvx case the mtime check cannot. Separately, document the dogfooding posture explicitly: when editing the engine, tracked writes must go through `clauderize ops` (fresh process, local source) or the session must be wired to the local editable command, and the CONTRIBUTING/dogfood docs should say which. Do NOT retro-edit H-26 -- it is append-only (INVARIANT-03) and now joins D-052/D-062/H-19/H-23 as acceptance evidence.
- **Resolution**: Resolved in Phase 0 of the 1.14.2 backlog gameplan. src/clauderizer/engine_identity.py answers the reflexive question by SELF-INTROSPECTION rather than a handshake: the process already knows where its own module was imported from and what version it carries, and the repo's src/ is on disk, so serving_build() compares them. Read-only and hook-safe (INVARIANT-06), and distinct from H-20's doctor probe, which spawns some OTHER registered command. Fires on the exact shape that caused this finding -- served from uv's cache while the tree sits elsewhere -- INCLUDING when both report the same version, since the path is what disambiguates and a version-only check would have called 1.14.1's development healthy. Silent for an ordinary consumer repo (no src/clauderizer to compare) and silent when the process IS the tree, so INVARIANT-08 byte-identity holds; verified live with zero warning lines against this working tree. 13 tests, behavioral RED on the pre-1.14.2 tree at 66d5efa (bundle key None, no digest line). One test deliberately pins that engine_source_newer_than returns False in this situation on BOTH trees, so nobody mistakes tightening the mtime check for fixing this. HONEST SCOPE, stated in the docstring: it reports on the process that EXECUTES it and nothing else -- a session whose MCP server and hook run different builds gets an independent answer from each. REMAINING DEPLOYMENT GAP, same shape as H-23's: the MCP server in a session is launched from the published build, so this detector only reaches that surface once 1.14.2 publishes.

### H-28 — release-check gates four registries and push ordering but never asks whether CI passed — a workflow-level green hiding a skipped matrix cell would sail straight through to a tag

- **Severity**: MEDIUM
- **Status**: resolved (2026-07-26)
- **Affected**: src/clauderizer/release_check.py — the nine checks cover clean tree, push ordering, four registries, the tag==source publish gate, and the README ritual mention; none of them touch CI.
- **Impact**: The exact 0.14.0/1.14.2 failure shape, one level up. Both shipped with Windows cells red, and L-51 sweep (2) says to run the suite on every host leg the CI matrix covers because a green on one OS is a guess about the others and the publish cannot be undone. 1.14.3 verified all 10 jobs at cell granularity — but only because the phase's exit criterion said to, by hand. Nothing in the ritual would have stopped a tag if a cell had been skipped or if only the workflow-level conclusion had been read. GitHub reports a workflow as `success` when a matrix cell is skipped, so the failure is silent by construction.
- **Root cause**: release-check was built to answer "is this version claimed anywhere and is origin ahead of the tag" (H-07/H-19). Whether the code being released actually passes on every platform it claims to support was left to the release ritual's prose. That split made sense when the registries were the observed failure; the observed failure has since moved to the matrix.
- **Reproduction**: Add `if: matrix.os != 'windows-latest'` to a matrix cell in .github/workflows/test.yml, push, and observe: `gh run list` reports the Tests workflow `success`, while `gh run view --json jobs` shows the windows cells absent. `clauderize release-check` reports OK either way.
- **Recommended fix**: Add a CI check to release-check that resolves the runs for the exact HEAD sha and asserts, at JOB granularity, that every job concluded `success` — explicitly failing on `skipped`, `cancelled` and on zero runs found. Follow the established three-state honesty (D-010): `unverifiable` when the host cannot reach the API or no CI is configured, never a false green. The job enumeration is the load-bearing part; reading the workflow conclusion is what this finding is about.
- **Regression tests**: None yet — the gap is the finding. A fix needs both directions per L-25: the check must go red on a skipped/failed cell and stay green on a fully-passing matrix, plus report `unverifiable` rather than green when the API is unreachable.
- **Resolution**: Resolved 2026-07-26 in 1.14.4. `release_check._ci_check` joins the ritual as "CI green on this commit (every job)", enumerating every job of every run for the exact HEAD sha via `_ci_runs_and_jobs`. The run's own `conclusion` is never consulted — that is the field which stays `success` when a matrix cell is skipped, and a test pins the property by feeding a `failure` run whose every job succeeded and asserting the verdict still comes from the job set alone. Green means every job concluded `success`; `skipped`/`cancelled`/`timed_out`/`failure`/`stale`/`startup_failure`/`neutral`/`action_required` fail and are named, a still-running workflow fails, and zero runs fails (the absence of a run is a definite fact about the code being tagged, not an unknown). `gh` missing or unreachable is `unverifiable`, no workflows is `skip` — three-state honesty per D-010, never a false green.

DIVERGENCE from this finding's own recommendation, recorded rather than quietly followed: the recommendation said to fail on skipped and cancelled, and that is what shipped — but only after checking the false-positive surface, which the recommendation did not consider. A repo with a job-level `if:` will go red on a legitimately conditional job. `unverifiable` was weighed as the softer verdict and rejected, because the CLI renders exit 3 as "OK with unverifiable check(s)" and a missing Windows cell must not read as OK. The trade is stated in the module docstring, the subsystem doc and the CHANGELOG; every offending job is named so the choice is actionable. This repo has no job-level conditionals.

Proven both directions per L-25: 16 new tests covering green, a skipped cell whose workflow says `success` (the H-28 case), a failed job, each non-success conclusion parametrized, still-running, no-run, jobless-but-completed, unreachable API, no-workflows, and the partial-read trap where `run list` succeeds but `run view` fails (which returns None rather than certifying a matrix it never enumerated). Standing oracle demonstrated behaviorally: the same repo with the same CI reality exits 0 on the pre-fix tree and 2 on this one, probed through `release_check.run()` alone — present on both trees, no ImportError. Docs updated in the same change: the subsystem doc's explicit "it cannot check CI" claim (written hours earlier in this same gameplan) is now the section describing the check, plus README's command line and RELEASING.md steps 3 and G3. Suite 1311 → 1327.

### H-29 — Write-guard removals are silent at write time — prose is altered with no echo to the writer

- **Severity**: low
- **Status**: resolved (2026-07-28)
- **Affected**: the write-guard seam applied to cz_* write bodies; refusals.jsonl (writer landed in P1, no reader yet — O-03)
- **Impact**: The unbalanced-closer guard correctly stripped three un-backticked tag names from a lesson's prose, leaving a sentence reading 'carrying , , and three fragments' in tracked memory; the writer only noticed by rereading the stored entry. The code-span exemption (L-63) works only if the writer remembers it at every write site that discusses markup — a habit the engine demands but never prompts for at the moment it matters.
- **Root cause**: The guard mutates the body before persist and returns success with no advisory naming the removed fragments, so the alteration is invisible at the exact moment the writer could cheaply fix it by re-writing with code spans.
- **Recommended fix**: Echo removals in the write result (advisory field naming the stripped fragments, nudging code-span quoting), and wire the refusals.jsonl reader (O-03, phase Attention and consolidation) so accumulated removals surface in cz_mine_failures / cz_corpus_health rather than staying a dormant evidence store.
- **Resolution**: Both recommended halves shipped: the refusals.jsonl reader landed in P3 (O-03: cz_mine_failures second source + corpus_health count), and the write-time echo ships in 2.0.0a1 — mutations gains a per-call contextvar accumulator (begin/drain_sanitizer_echo) the guard notes removals into, and ops' innermost REGISTRY wrapper (_echoed) attaches a sanitizer_advisory naming the stripped fragments with the code-span rewrite nudge on every transport; advisory-only, write untouched. Pinned by tests/test_sanitizer_echo.py (echo, code-span silence, per-op reset, never-blocks), armed red-first via TDD
### H-30 — Version stamps ratchet backward when a modernize/apply pass runs on a serving engine older than the tree — observed live: cz_modernize (published 1.14.3, procedure 1.9.0) restamped config 1.11.0 → 1.9.0

- **Severity**: medium
- **Status**: resolved (2026-07-28)
- **Affected**: src/clauderizer/modernize.py (apply/stamp_procedure_version); .mcp.json portable wiring path
- **Invariant violated**: INVARIANT-07 (parity: the published-engine MCP surface silently lags the tree's tool set mid-alpha)
- **Impact**: Any stamp/migration op executed through a stale serving engine silently regresses recorded state: this session's MCP server is the published engine (portable uvx wiring per D-031, correct for the mid-alpha dogfood repo) while the hook and tree run 2.0-alpha — cz_modernize(apply) restamped procedure_version BACKWARD from 1.11.0 to 1.9.0. Symmetric hazard for any future stamping op. A split-brain session (hook=tree, MCP=published) also means new ops (cz_reinforce_lesson) are unservable until publish.
- **Root cause**: modernize.apply stamps the SERVING engine's PROCEDURE_VERSION unconditionally — no monotonicity guard — and nothing in the op result declares which engine build served the write (the workflow-critique gameplan's engine-identity premise, now with live evidence).
- **Reproduction**: In a repo whose tree engine is ahead of the published one: call cz_modernize(apply=true) through MCP wired via the portable uvx command; observe config.procedure_version move backward. Recovery: rerun `.venv/bin/clauderize upgrade` with the tree engine.
- **Recommended fix**: Two-part fix owned by the engine-identity phase (2026-07-24 workflow-critique gameplan): (1) stamp monotonicity — stamp_procedure_version refuses to move a semver stamp downward (report-only advisory when serving &lt; stamped); (2) doctor/op results certify the serving build (version + origin) so an agent can detect serving-vs-tree drift before mutating. Interim discipline: run stamps via the repo venv CLI, never the portable MCP, when the tree is ahead.
- **Resolution**: Monotonicity half shipped in 2.0.0a1: modernize refuses the downward stamp at BOTH seams — report() surfaces an engine_older_than_stamp advisory proposal instead of the mechanical action, and apply() refuses the downward write even for a confused caller; unparseable stamps still read as legacy and stamp forward. Pinned by tests/test_stamp_monotonicity.py (downward-refused+surfaced, upward-still-applies, junk-forward), armed red-first via TDD; recovery of the live casualty already done (stamp restored 1.12.0 via tree CLI). The serving-build-certification half (op results naming which engine served them) is explicitly routed to the 2026-07-24 workflow-critique gameplan's phase 0 "Engine identity — doctor certifies what it launched", which now holds this finding as live prior evidence

### H-31 — mcp 2.0.0 (released 2026-07-28) removed mcp.server.fastmcp — every fresh clauderizer[mcp] install worldwide broke same-day; repo pinned mcp&lt;2, published 1.14.4 remains broken pending a hotfix decision

- **Severity**: high
- **Status**: open (2026-07-28)
- **Affected**: pyproject [project.optional-dependencies] mcp; published clauderizer==1.14.4 on PyPI; .github Quickstart job
- **Impact**: Any fresh resolution of clauderizer[mcp] (uvx quickstart, new installs, CI) after 13:45 UTC gets mcp 2.0.0 and the MCP server dies at import (no serverInfo from the initialize handshake). The repo side is fixed (mcp>=1.2,&lt;2 at 8fc9c22; fresh-resolve proof: 1.29.0, fastmcp imports), but the PUBLISHED 1.14.4 has the open constraint — the Quickstart CI job (which tests the published package) stays red and real users stay broken until a fixed stable version publishes.
- **Root cause**: Unbounded upper constraint (mcp>=1.2) on an SDK that shipped a breaking major; detection worked exactly as designed — doctor's capability-based MCP identity probe (H-20/D-060) and the H-28 job-granularity CI gate both fired on the first affected runs.
- **Recommended fix**: Cut 1.14.5 = v1.14.4 + the pin only (branch from the tag, PR for matrix evidence, tag, Release, publish) so the stable channel heals; ship 2.0.0a1 after. Follow-up: adapt to the mcp 2.x SDK deliberately, then lift the pin with tests against both majors.
- **Resolution**: Hotfix half SHIPPED 2026-07-28: v1.14.5 (= staged-1.14.4 content + mcp>=1.2,&lt;2) published to PyPI with 10/10 matrix evidence on the exact sha; cold uvx resolve + doctor identity handshake verified healed; release/1.14.x is the stable line's home. REMAINS OPEN for the second half: deliberate adaptation to the mcp 2.x SDK (then lift the pin with tests against both majors) — unowned by any gameplan yet; candidate scope beside the workflow-critique engine-identity work

### H-32 — A hook wrapper invoking a nonexistent absolute path reports advisory (exit 3), not drift — a repo can lose its digest indefinitely while doctor stays green-ish

- **Severity**: medium
- **Status**: open (2026-07-30)
- **Affected**: cli.py doctor hook-wrapper freshness check (the branch emitting "wrapper invokes ... but a fresh init would compose ..."); .clauderizer/hook.sh wrappers carrying an absolute engine path
- **Invariant violated**: The exit-3 contract: "?" means a check could not be VERIFIED from this host, never that a check failed. Whether an absolute path exists on this filesystem is definitely verifiable, so classifying it "?" spends the unverifiable state on a known-bad fact — the same reasoning H-28 applied to CI ("the absence of a run is a definite fact about the code, not an unknown").
- **Preconditions**: A repo whose wiring was generated on another machine or under another user account, then cloned/moved (committed hook.sh with an absolute engine path). Any repo that travels between hosts is exposed; the portable `uvx --from clauderizer` form is not, which is why only one of five swept repos was affected.
- **Impact**: Found live in ~/phasekeep during the 2.0.0 fleet sweep: its wrapper invoked /home/operator/.local/bin/uvx — a path from a different machine/user, absent on this host. Every session got "[Clauderizer] repo unreachable: /home/operator/phasekeep" instead of a digest, and two open gameplans (m35-installers, m4-announcement, both with blocked phases) were invisible for an unknown period. clauderize doctor reported exit 3 with ZERO drift markers: the freshness check surfaced the mismatch as "? hook wrapper freshness — wrapper invokes X but a fresh init would compose Y ... (expected with a custom --run-cmd)", which reads as a benign customization note. The digest is the product's primary delivery surface, so this is a silent total loss of the feature with no failing signal.
- **Root cause**: The freshness check compares the wrapper's composed command against what a fresh init WOULD compose, and treats any difference as an expected customization ("expected with a custom --run-cmd"). It never asks the cheaper, stronger question: does the executable this wrapper actually invokes exist and run here? A legitimate --run-cmd customization and a dead cross-machine path are indistinguishable to a string comparison, so the benign reading wins for both.
- **Reproduction**: In any clauderized repo, rewrite .clauderizer/hook.sh's engine-hook path to an absolute path under a nonexistent home (e.g. /home/nobody/.local/bin/uvx), then: `bash .clauderizer/hook.sh` emits "[Clauderizer] repo unreachable: ..." (exit 0), while `clauderize doctor` exits 3 with no ✗ line. Observed verbatim in ~/phasekeep before repair at 909c07b.
- **Recommended fix**: In the freshness branch, when the wrapper's invoked executable is an ABSOLUTE path, stat it: a nonexistent target is drift (✗, exit 2) with the path named, not an advisory — absence is a definite fact, not an unknown. Keep "?" for a genuine --run-cmd divergence whose target does exist. Consider also having the hook's own "repo unreachable" breadcrumb be something doctor can observe, so the delivery surface and the diagnostic agree. Second-order: prefer emitting the portable `uvx --from clauderizer` form over an absolute local-tool path for committed wiring — an absolute path is machine-specific by construction and is what created this class.
- **Regression tests**: A doctor test with a wrapper pointing at a nonexistent absolute path asserting exit 2 and a ✗ naming the path; a companion test with an existing custom --run-cmd target asserting the "?" advisory survives unchanged (so the fix does not turn every legitimate customization red).
